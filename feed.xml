<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="ko"><generator uri="https://jekyllrb.com/" version="4.3.4">Jekyll</generator><link href="https://lkhlkh23.github.io/jekyll-theme-yat/feed.xml" rel="self" type="application/atom+xml" /><link href="https://lkhlkh23.github.io/jekyll-theme-yat/" rel="alternate" type="text/html" hreflang="ko" /><updated>2024-10-01T13:33:04+00:00</updated><id>https://lkhlkh23.github.io/jekyll-theme-yat/feed.xml</id><title type="html">Code Chronicles</title><subtitle>Write an awesome description for your new site here. You can edit this line in _config.yml. It will appear in your document head meta (for Google search results) and in your feed.xml site description.</subtitle><author><name>lkhlkh23</name></author><entry><title type="html">K6 + InfluxDB + Prometheus + Grafana 를 이용한 성능테스트</title><link href="https://lkhlkh23.github.io/jekyll-theme-yat/test/2024/10/01/k6-load-test.html" rel="alternate" type="text/html" title="K6 + InfluxDB + Prometheus + Grafana 를 이용한 성능테스트" /><published>2024-10-01T00:00:00+00:00</published><updated>2024-10-01T00:00:00+00:00</updated><id>https://lkhlkh23.github.io/jekyll-theme-yat/test/2024/10/01/k6-load-test</id><content type="html" xml:base="https://lkhlkh23.github.io/jekyll-theme-yat/test/2024/10/01/k6-load-test.html"><![CDATA[<p>이번에는 K6 에 대해 알아보려고 한다! 처음에는 K3 로 검색을 했는데, 기아자동차가 나왔다. 그래서 K4, 5, 6 순차적으로 검색을 해보니, 내가 원하는 성능테스트 도구에 대한 결과가 나왔다.</p>

<p>오늘은 K6 를 이용한 테스트 방법과 InfluxDB, Grafana, Prometheus 와 결합해서 사용하는 방법 중심으로 정리하겠다!</p>

<h3 id="what-is-k6">What is K6..?</h3>

<p>K6 는 <code class="language-plaintext highlighter-rouge">InfluxDB, Grafana, Prometheus</code> 등 다양한 도구 및 서비스와 쉽게 통합되어 모니터링이 가능하기 때문에 많이 사용되는 <code class="language-plaintext highlighter-rouge">부하 테스트 도구</code>이다. JS 로 작성된 스크립트를 사용하여 복잡한 테스트 시나리오를 생성할 수 있다.</p>

<p>K6 는 HTTP 기반 테스트에서 뛰어난 성능을 발휘하지만, WebSocket, TCP 프로토콜에 대해서는 제한적인 기능만 제공하고 있다. 또한, 관련 서비스에 대한 러닝커브가 있다는 단점이 있다.</p>

<h3 id="how-to-use-k6">How to use K6..?</h3>

<p><strong>Step.1 Install K6</strong></p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>brew <span class="nb">install </span>k6
</code></pre></div></div>

<p><strong>Step.2 Prepare script</strong></p>

<div class="language-jsx highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">import</span> <span class="nx">http</span> <span class="k">from</span> <span class="dl">"</span><span class="s2">k6/http</span><span class="dl">"</span><span class="p">;</span>
<span class="k">import</span> <span class="p">{</span> <span class="nx">sleep</span><span class="p">,</span> <span class="nx">check</span> <span class="p">}</span> <span class="k">from</span> <span class="dl">"</span><span class="s2">k6</span><span class="dl">"</span><span class="p">;</span>

<span class="k">export</span> <span class="kd">const</span> <span class="nx">options</span> <span class="o">=</span> <span class="p">{</span>
    <span class="c1">// 지속시간에 따른 사용자수 지정 가능</span>
    <span class="na">stages</span><span class="p">:</span> <span class="p">[</span>
        <span class="p">{</span> <span class="na">duration</span><span class="p">:</span> <span class="dl">'</span><span class="s1">2s</span><span class="dl">'</span><span class="p">,</span> <span class="na">target</span><span class="p">:</span> <span class="mi">5</span> <span class="p">},</span>
        <span class="p">{</span> <span class="na">duration</span><span class="p">:</span> <span class="dl">'</span><span class="s1">10s</span><span class="dl">'</span><span class="p">,</span> <span class="na">target</span><span class="p">:</span> <span class="mi">5</span> <span class="p">},</span>
        <span class="p">{</span> <span class="na">duration</span><span class="p">:</span> <span class="dl">'</span><span class="s1">3s</span><span class="dl">'</span><span class="p">,</span> <span class="na">target</span><span class="p">:</span> <span class="mi">10</span> <span class="p">},</span>
        <span class="p">{</span> <span class="na">duration</span><span class="p">:</span> <span class="dl">'</span><span class="s1">5ms</span><span class="dl">'</span><span class="p">,</span> <span class="na">target</span><span class="p">:</span> <span class="mi">10</span> <span class="p">},</span>
        <span class="p">{</span> <span class="na">duration</span><span class="p">:</span> <span class="dl">'</span><span class="s1">3s</span><span class="dl">'</span><span class="p">,</span> <span class="na">target</span><span class="p">:</span> <span class="mi">0</span> <span class="p">},</span>
    <span class="p">],</span>

    <span class="c1">// 응답 95번째 백분위수에 100ms 이하를 요구</span>
    <span class="na">thresholds</span><span class="p">:</span> <span class="p">{</span>
        <span class="na">http_req_duration</span><span class="p">:</span> <span class="p">[</span><span class="dl">'</span><span class="s1">p(95)&lt;100</span><span class="dl">'</span><span class="p">],</span>
    <span class="p">},</span>

    <span class="na">ext</span><span class="p">:</span> <span class="p">{</span> 
        <span class="na">loadimpact</span><span class="p">:</span> <span class="p">{</span>
            <span class="na">projectID</span><span class="p">:</span> <span class="mi">1234</span><span class="p">,</span> 
            <span class="na">name</span><span class="p">:</span> <span class="dl">"</span><span class="s2">Load Test</span><span class="dl">"</span><span class="p">,</span>
        <span class="p">},</span>
    <span class="p">}</span>
<span class="p">};</span>

<span class="c1">// 테스트 대상</span>
<span class="kd">const</span> <span class="nx">urls</span> <span class="o">=</span> <span class="p">[</span>
    <span class="dl">"</span><span class="s2">http://localhost:8081/coupons/1</span><span class="dl">"</span><span class="p">,</span>
    <span class="dl">"</span><span class="s2">http://localhost:8081/coupons/2</span><span class="dl">"</span><span class="p">,</span>
    <span class="dl">"</span><span class="s2">http://localhost:8081/coupons/3</span><span class="dl">"</span><span class="p">,</span>
<span class="p">];</span>

<span class="k">export</span> <span class="k">default</span> <span class="nf">function </span><span class="p">()</span> <span class="p">{</span>
    <span class="kd">const</span> <span class="nx">url</span> <span class="o">=</span> <span class="nx">urls</span><span class="p">[</span><span class="nb">Math</span><span class="p">.</span><span class="nf">floor</span><span class="p">(</span><span class="nb">Math</span><span class="p">.</span><span class="nf">random</span><span class="p">()</span> <span class="o">*</span> <span class="nx">urls</span><span class="p">.</span><span class="nx">length</span><span class="p">)];</span>
    <span class="kd">const</span> <span class="nx">response</span> <span class="o">=</span> <span class="nx">http</span><span class="p">.</span><span class="nf">get</span><span class="p">(</span><span class="nx">url</span><span class="p">);</span>
    <span class="nf">check</span><span class="p">(</span><span class="nx">response</span><span class="p">,</span> <span class="p">{</span>
        <span class="dl">"</span><span class="s2">success</span><span class="dl">"</span><span class="p">:</span> <span class="p">(</span><span class="nx">res</span><span class="p">)</span> <span class="o">=&gt;</span> <span class="nx">res</span><span class="p">.</span><span class="nx">status</span> <span class="o">===</span> <span class="mi">200</span><span class="p">,</span>
			  <span class="dl">"</span><span class="s2">fail</span><span class="dl">"</span><span class="p">:</span> <span class="p">(</span><span class="nx">res</span><span class="p">)</span> <span class="o">=&gt;</span> <span class="nx">res</span><span class="p">.</span><span class="nx">status</span> <span class="o">!=</span> <span class="mi">200</span><span class="p">,</span>
    <span class="p">});</span>
<span class="p">}</span>
</code></pre></div></div>

<p><strong>Step.3 Execute load test</strong></p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>k6 run k6-load-test.js
</code></pre></div></div>

<p><img src="https://raw.githubusercontent.com/lkhlkh23/lkhlkh23.github.io/master/images/2024-10-01/0.png" alt="0.png" /></p>

<p>터미널에서 결과를 확인할 수 있다. K6 를 사용하는 이유는 Grafana 와의 호환성이니, Grafana 와 연동해보자!</p>

<h3 id="how-to-combine-k6-influxdb-and-grafana">How to combine K6, InfluxDB and Grafana</h3>

<p><strong>Step.1 Run InfluxDB and Grafana</strong></p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>version: <span class="s1">'3.4'</span>

services:
  influxdb:
    image: influxdb:1.8
    ports:
      - <span class="s2">"8086:8086"</span>
    environment:
      - <span class="nv">INFLUXDB_HTTP_AUTH_ENABLED</span><span class="o">=</span><span class="nb">false</span>
      - <span class="nv">INFLUXDB_DB</span><span class="o">=</span>k6
  grafana:
    image: grafana/grafana:latest
    ports:
      - <span class="s2">"3000:3000"</span>
    environment:
      - <span class="nv">GF_AUTH_ANONYMOUS_ORG_ROLE</span><span class="o">=</span>Admin
      - <span class="nv">GF_AUTH_ANONYMOUS_ENABLED</span><span class="o">=</span><span class="nb">true</span>
      - <span class="nv">GF_AUTH_BASIC_ENABLED</span><span class="o">=</span><span class="nb">false
    </span>volumes:
      - ./grafana:/etc/grafana/provisioning/
</code></pre></div></div>

<p>docker-compose 를 이용해서, InfluxDB 와 Grafana 를 실행한다.</p>

<p><strong>Step.2 Execute load test</strong></p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>k6 run <span class="nt">--out</span> <span class="nv">influxdb</span><span class="o">=</span>http://localhost:8086/k6 k6-load-test.js
</code></pre></div></div>

<p><strong>Step.3 Access Grafana</strong></p>

<ul>
  <li>id : admin</li>
  <li>password : admin</li>
</ul>

<p><strong>Step.4 Connect InfluxDB</strong></p>

<p><img src="https://raw.githubusercontent.com/lkhlkh23/lkhlkh23.github.io/master/images/2024-10-01/1.png" alt="1.png" /></p>

<p><img src="https://raw.githubusercontent.com/lkhlkh23/lkhlkh23.github.io/master/images/2024-10-01/2.png" alt="2.png" /></p>

<p><strong>Step.5 Create Dashboard</strong></p>

<p><img src="https://raw.githubusercontent.com/lkhlkh23/lkhlkh23.github.io/master/images/2024-10-01/3.png" alt="3.png" /></p>

<p><img src="https://raw.githubusercontent.com/lkhlkh23/lkhlkh23.github.io/master/images/2024-10-01/4.png" alt="4.png" /></p>

<p>미리 정의된 Dashboard 포맷이 많이 있으니, 아래 링크를 통해 필요한 포맷을 가져다 쓰자! (id : 2587)</p>

<ul>
  <li>https://grafana.com/grafana/dashboards/</li>
</ul>

<p><img src="https://raw.githubusercontent.com/lkhlkh23/lkhlkh23.github.io/master/images/2024-10-01/5.png" alt="5.png" /></p>

<p><img src="https://raw.githubusercontent.com/lkhlkh23/lkhlkh23.github.io/master/images/2024-10-01/6.png" alt="6.png" /></p>

<h3 id="how-to-combine-k6-prometheus-and-grafana">How to combine K6, Prometheus and Grafana</h3>

<p><strong>Step.1 Run Prometheus</strong></p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>scrape_configs:
  - job_name: <span class="s1">'spring-boot'</span>
    metrics_path: <span class="s1">'/actuator/prometheus'</span>
    scrape_interval: 5s
    static_configs:
      - targets: <span class="o">[</span><span class="s1">'host.docker.internal:8081'</span><span class="o">]</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">targets</code> 에는 Prmetheus 에서 Metric 을 수집할 대상 서버 주소를 설정하면 된다! docker 를 사용하고 있다면, 127.0.0.1 이 아닌,  <code class="language-plaintext highlighter-rouge">host.docker.internal</code> 을 사용해라!</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>version: <span class="s1">'3.4'</span>

services:
  prometheus:
    image: prom/prometheus:latest
    container_name: prometheus
    ports:
      - <span class="s2">"9090:9090"</span>
    volumes:
       - ./prometheus.yml:/etc/prometheus/prometheus.yml
  influxdb:
    image: influxdb:1.8
    ports:
      - <span class="s2">"8086:8086"</span>
    environment:
      - <span class="nv">INFLUXDB_HTTP_AUTH_ENABLED</span><span class="o">=</span><span class="nb">false</span>
      - <span class="nv">INFLUXDB_DB</span><span class="o">=</span>k6

  grafana:
    image: grafana/grafana:latest
    ports:
      - <span class="s2">"3000:3000"</span>
    environment:
      - <span class="nv">GF_AUTH_ANONYMOUS_ORG_ROLE</span><span class="o">=</span>Admin
      - <span class="nv">GF_AUTH_ANONYMOUS_ENABLED</span><span class="o">=</span><span class="nb">true</span>
      - <span class="nv">GF_AUTH_BASIC_ENABLED</span><span class="o">=</span><span class="nb">false
    </span>volumes:
      - ./grafana:/etc/grafana/provisioning/
</code></pre></div></div>

<p><strong>Step.2 Configure prometheus settings in target project</strong></p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">server</span><span class="pi">:</span>
  <span class="na">port</span><span class="pi">:</span> <span class="m">8081</span>

<span class="na">spring</span><span class="pi">:</span>
  <span class="na">application</span><span class="pi">:</span>
    <span class="na">name</span><span class="pi">:</span> <span class="s">coupon</span>

<span class="na">management</span><span class="pi">:</span>
  <span class="na">endpoints</span><span class="pi">:</span>
    <span class="na">web</span><span class="pi">:</span>
      <span class="na">exposure</span><span class="pi">:</span>
        <span class="na">include</span><span class="pi">:</span> <span class="s">health, prometheus</span>

</code></pre></div></div>

<div class="language-groovy highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">implementation</span> <span class="s1">'io.micrometer:micrometer-registry-prometheus'</span>
<span class="n">implementation</span> <span class="s1">'org.springframework.boot:spring-boot-starter-actuator'</span>
</code></pre></div></div>

<p>위와 같이 설정하고 아래 API 를 실행해서 정상적으로 노출되는지 확인해보자!</p>

<ul>
  <li>http://localhost:8081/actuator/prometheus</li>
  <li>http://localhost:8081/actuator/health</li>
</ul>

<p><strong>Step.3 Check the status of target servers in Prometheus</strong></p>

<p><img src="https://raw.githubusercontent.com/lkhlkh23/lkhlkh23.github.io/master/images/2024-10-01/7.png" alt="7.png" /></p>

<p>Prometheus 에 접속해서 대상 서버의 상태를 확인해보자! Status 가 UP 이면 성공!</p>

<ul>
  <li>http://localhost:9090/targets?search=</li>
</ul>

<p><strong>Step.4 Connect Prometheus</strong></p>

<p><img src="https://raw.githubusercontent.com/lkhlkh23/lkhlkh23.github.io/master/images/2024-10-01/8.png" alt="8.png" /></p>

<p><img src="https://raw.githubusercontent.com/lkhlkh23/lkhlkh23.github.io/master/images/2024-10-01/9.png" alt="9.png" /></p>

<p><strong>Step.5 Create Dashboard</strong></p>

<p><img src="https://raw.githubusercontent.com/lkhlkh23/lkhlkh23.github.io/master/images/2024-10-01/10.png" alt="10.png" /></p>

<p>이번에도 미리 정의된 포맷을 사용하려고 한다. (id : 11378)</p>

<p><img src="https://raw.githubusercontent.com/lkhlkh23/lkhlkh23.github.io/master/images/2024-10-01/11.png" alt="11.png" /></p>

<p><img src="https://raw.githubusercontent.com/lkhlkh23/lkhlkh23.github.io/master/images/2024-10-01/2.png" alt="12.png" /></p>

<p>K6 를 이용해서 API 를 성능테스트를 위한 스크립트 및 환경을 세팅해보았다! InfluxDB 와 Grafana 를 이용해서 성능 테스트 결과를 시각화했고, Prometheus 와 Grafana 를 이용해서 성능테스트를 진행하는 과정에서 인스턴스의 상태를 시각화했다!</p>

<p>지금까지 vegeta, nGrinder, K6 를 사용했지만, 개인적으로 K6 가 성능테스트 결과와 대상 서버를 함께 시각화할 수 있어서 가장 좋았다!</p>

<h3 id="and-i-am">And I am..?!</h3>

<p>나는 서버 개발자인가?! 테스터인가?! 정체성 혼란이 왔다!</p>

<p><img src="https://raw.githubusercontent.com/lkhlkh23/lkhlkh23.github.io/master/images/2024-10-01/13.png" alt="13.png" /></p>

<p>하지만, 개인적으로 코드의 품질과 성능을 고려하지 않고 기능만 개발하는 개발자는 IT 가 아닌, 제조업을 종사한다고 생각한다. 성능 테스트를 이해하고 인지하고 있다면, 적어도 성능을 조금이라도 고려할 수 있다고 생각한다.</p>

<p>그래서 vegeta, nGrinder 그리고 K6 를 공부한것이 시간낭비는 아닐것이다.. 그렇지?!</p>]]></content><author><name>lkhlkh23</name></author><category term="test" /><category term="test" /><category term="performance" /><summary type="html"><![CDATA[이번에는 K6 에 대해 알아보려고 한다! 처음에는 K3 로 검색을 했는데, 기아자동차가 나왔다. 그래서 K4, 5, 6 순차적으로 검색을 해보니, 내가 원하는 성능테스트 도구에 대한 결과가 나왔다.]]></summary></entry><entry><title type="html">MDC, Servlet Container, ThreadPool</title><link href="https://lkhlkh23.github.io/jekyll-theme-yat/springboot/2024/09/30/mdc.html" rel="alternate" type="text/html" title="MDC, Servlet Container, ThreadPool" /><published>2024-09-30T00:00:00+00:00</published><updated>2024-09-30T00:00:00+00:00</updated><id>https://lkhlkh23.github.io/jekyll-theme-yat/springboot/2024/09/30/mdc</id><content type="html" xml:base="https://lkhlkh23.github.io/jekyll-theme-yat/springboot/2024/09/30/mdc.html"><![CDATA[<p>팀원이 외부 API 호출 이력을 남기는 개발을 진행을 하고있는데, MDC 를 이용한다고 들었다. 생소한 단어였다. MDC 활용에 대해 어떤 코멘트도 할 수 없었다. 그래서 정리하려고 한다.</p>

<p><img src="https://raw.githubusercontent.com/lkhlkh23/lkhlkh23.github.io/master/images/2024-09-30/banner.png" alt="banner.png" /></p>

<h3 id="what-is-mdc">What is MDC..?</h3>

<p>MDC (Mapped Diagnostic Context) 는 로깅 시스템에서 각 스레드에 대한 컨텍스트 정보 저장을 위해 사용한다. 주로 Apache Log4j 와 같은 로깅 프레임워크에서 사용된다.</p>

<blockquote>
  <p>Mapped Diagnostic Context</p>

  <p>Mapped :  로그에 추가적인 정보를 제공위해 Key 와 Value 를 연결하여 저장<br />
Diagnostic : 로그에 추가적인 정보를 포함시킴으로써 문제를 식별하고 분석하는 과정을 의미<br />
Context : 스레드별로 독립적인 컨텍스트를 제공하여 로그가 어떤 상황에서 발생했는지를 명확하게 이해</p>

</blockquote>

<p>MDC  는 각 스레드에서 발생하는 로그에 대한 추가적인 진단 정보를 저장하고 이를 로그에 포함시킴으로써, 문제를 보다 쉽게 추적하고 진단할 수 있게 한다. MDC 는 <code class="language-plaintext highlighter-rouge">ThreadLocal</code> 을 사용하여 각 스레드별로 독립적인 Map 을 생성한다. Servlet Container 에서는 Thread Pool 에서 스레드를 미리 생성해서 재사용하고 있기 때문에 스레드 반납전에 MDC 를 반드시 클리어해야 한다. 각각의 스레드는 자신만의 MDC 를 가지고 있기 때문에 멀티스레드 환경에서도 <code class="language-plaintext highlighter-rouge">Thread-Safe</code> 하다.</p>

<h3 id="mdc-development">MDC development</h3>

<p>FeignTrace 커스텀 어노테이션이 붙은 메소드 호출에 대해서 AOP 를 설정한다. 호출 전에는 MDC 에 값을 저장하고, 호출 후에는 MDC 에 값을 출력하는 방식으로 간단하게 예제 코드를 작성했다!</p>

<p>제일 중요한것은 API 호출 성공과 실패 모두 <code class="language-plaintext highlighter-rouge">MDC 를 clear</code> 해야한다!</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">package</span> <span class="nn">client.coupon.config</span><span class="o">;</span>

<span class="kn">import</span> <span class="nn">java.lang.annotation.ElementType</span><span class="o">;</span>
<span class="kn">import</span> <span class="nn">java.lang.annotation.Retention</span><span class="o">;</span>
<span class="kn">import</span> <span class="nn">java.lang.annotation.RetentionPolicy</span><span class="o">;</span>
<span class="kn">import</span> <span class="nn">java.lang.annotation.Target</span><span class="o">;</span>

<span class="nd">@Retention</span><span class="o">(</span><span class="nc">RetentionPolicy</span><span class="o">.</span><span class="na">RUNTIME</span><span class="o">)</span>
<span class="nd">@Target</span><span class="o">(</span><span class="nc">ElementType</span><span class="o">.</span><span class="na">METHOD</span><span class="o">)</span>
<span class="kd">public</span> <span class="nd">@interface</span> <span class="nc">FeignTrace</span> <span class="o">{</span>
<span class="o">}</span>
</code></pre></div></div>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">package</span> <span class="nn">client.coupon.config</span><span class="o">;</span>

<span class="kn">import</span> <span class="nn">org.aspectj.lang.ProceedingJoinPoint</span><span class="o">;</span>
<span class="kn">import</span> <span class="nn">org.aspectj.lang.annotation.After</span><span class="o">;</span>
<span class="kn">import</span> <span class="nn">org.aspectj.lang.annotation.Around</span><span class="o">;</span>
<span class="kn">import</span> <span class="nn">org.aspectj.lang.annotation.Aspect</span><span class="o">;</span>
<span class="kn">import</span> <span class="nn">org.aspectj.lang.annotation.Before</span><span class="o">;</span>
<span class="kn">import</span> <span class="nn">org.slf4j.MDC</span><span class="o">;</span>
<span class="kn">import</span> <span class="nn">org.springframework.stereotype.Component</span><span class="o">;</span>

<span class="kn">import</span> <span class="nn">jakarta.servlet.http.HttpServletRequest</span><span class="o">;</span>
<span class="kn">import</span> <span class="nn">lombok.RequiredArgsConstructor</span><span class="o">;</span>
<span class="kn">import</span> <span class="nn">lombok.extern.slf4j.Slf4j</span><span class="o">;</span>

<span class="nd">@Aspect</span>
<span class="nd">@Component</span>
<span class="nd">@RequiredArgsConstructor</span>
<span class="nd">@Slf4j</span>
<span class="kd">public</span> <span class="kd">class</span> <span class="nc">FeignTraceAspect</span> <span class="o">{</span>

	<span class="kd">private</span> <span class="kd">final</span> <span class="nc">HttpServletRequest</span> <span class="n">request</span><span class="o">;</span>

	<span class="nd">@Around</span><span class="o">(</span><span class="s">"@annotation(FeignTrace)"</span><span class="o">)</span>
	<span class="kd">public</span> <span class="nc">Object</span> <span class="nf">trace</span><span class="o">(</span><span class="nc">ProceedingJoinPoint</span> <span class="n">joinPoint</span><span class="o">)</span> <span class="kd">throws</span> <span class="nc">Throwable</span> <span class="o">{</span>
		<span class="k">try</span> <span class="o">{</span>
			<span class="no">MDC</span><span class="o">.</span><span class="na">put</span><span class="o">(</span><span class="s">"parameters"</span><span class="o">,</span> <span class="n">extractParameters</span><span class="o">(</span><span class="n">joinPoint</span><span class="o">.</span><span class="na">getArgs</span><span class="o">()));</span>
			<span class="no">MDC</span><span class="o">.</span><span class="na">put</span><span class="o">(</span><span class="s">"path"</span><span class="o">,</span> <span class="n">request</span><span class="o">.</span><span class="na">getRequestURI</span><span class="o">());</span>
			<span class="no">MDC</span><span class="o">.</span><span class="na">put</span><span class="o">(</span><span class="s">"ip"</span><span class="o">,</span> <span class="n">request</span><span class="o">.</span><span class="na">getRemoteAddr</span><span class="o">());</span>
			<span class="kd">final</span> <span class="nc">Object</span> <span class="n">result</span> <span class="o">=</span> <span class="n">joinPoint</span><span class="o">.</span><span class="na">proceed</span><span class="o">();</span>
			<span class="n">log</span><span class="o">.</span><span class="na">info</span><span class="o">(</span><span class="s">"[success] ip : {}, path : {}, parameters : {}"</span><span class="o">,</span> <span class="no">MDC</span><span class="o">.</span><span class="na">get</span><span class="o">(</span><span class="s">"ip"</span><span class="o">),</span> <span class="no">MDC</span><span class="o">.</span><span class="na">get</span><span class="o">(</span><span class="s">"path"</span><span class="o">),</span> <span class="no">MDC</span><span class="o">.</span><span class="na">get</span><span class="o">(</span><span class="s">"parameters"</span><span class="o">));</span>
			<span class="k">return</span> <span class="n">result</span><span class="o">;</span>
		<span class="o">}</span> <span class="k">catch</span> <span class="o">(</span><span class="nc">Throwable</span> <span class="n">throwable</span><span class="o">)</span> <span class="o">{</span>
			<span class="n">log</span><span class="o">.</span><span class="na">error</span><span class="o">(</span><span class="s">"[fail] ip : {}, path : {}, parameters : {}"</span><span class="o">,</span> <span class="no">MDC</span><span class="o">.</span><span class="na">get</span><span class="o">(</span><span class="s">"ip"</span><span class="o">),</span> <span class="no">MDC</span><span class="o">.</span><span class="na">get</span><span class="o">(</span><span class="s">"path"</span><span class="o">),</span> <span class="no">MDC</span><span class="o">.</span><span class="na">get</span><span class="o">(</span><span class="s">"parameters"</span><span class="o">));</span>
			<span class="k">throw</span> <span class="n">throwable</span><span class="o">;</span>
		<span class="o">}</span> <span class="k">finally</span> <span class="o">{</span>
			<span class="no">MDC</span><span class="o">.</span><span class="na">clear</span><span class="o">();</span>
		<span class="o">}</span>
	<span class="o">}</span>

	<span class="kd">private</span> <span class="nc">String</span> <span class="nf">extractParameters</span><span class="o">(</span><span class="kd">final</span> <span class="nc">Object</span><span class="o">[]</span> <span class="n">arguments</span><span class="o">)</span> <span class="o">{</span>
		<span class="kd">final</span> <span class="nc">StringBuilder</span> <span class="n">parameters</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">StringBuilder</span><span class="o">();</span>
		<span class="k">for</span> <span class="o">(</span><span class="kd">final</span> <span class="nc">Object</span> <span class="n">arg</span> <span class="o">:</span> <span class="n">arguments</span><span class="o">)</span> <span class="o">{</span>
			<span class="n">parameters</span><span class="o">.</span><span class="na">append</span><span class="o">((</span><span class="nc">String</span><span class="o">)</span> <span class="n">arg</span><span class="o">);</span>
		<span class="o">}</span>

		<span class="k">return</span> <span class="n">parameters</span><span class="o">.</span><span class="na">toString</span><span class="o">();</span>
	<span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@Service</span>
<span class="nd">@Slf4j</span>
<span class="kd">public</span> <span class="kd">class</span> <span class="nc">FeignService</span> <span class="o">{</span>

	<span class="nd">@FeignTrace</span>
	<span class="kd">public</span> <span class="kt">void</span> <span class="nf">validate</span><span class="o">(</span><span class="kd">final</span> <span class="nc">String</span> <span class="n">id</span><span class="o">)</span> <span class="kd">throws</span> <span class="nc">Exception</span> <span class="o">{</span>
		<span class="k">if</span> <span class="o">(</span><span class="s">"1"</span><span class="o">.</span><span class="na">equals</span><span class="o">(</span><span class="n">id</span><span class="o">))</span> <span class="o">{</span>
			<span class="n">log</span><span class="o">.</span><span class="na">error</span><span class="o">(</span><span class="s">"error is occurred"</span><span class="o">);</span>
			<span class="k">throw</span> <span class="k">new</span> <span class="nf">Exception</span><span class="o">(</span><span class="s">"Invalid coupon id"</span><span class="o">);</span>
		<span class="o">}</span>
	<span class="o">}</span>

<span class="o">}</span>
</code></pre></div></div>

<div class="language-xml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cp">&lt;?xml version="1.0" encoding="UTF-8" ?&gt;</span>
<span class="nt">&lt;configuration&gt;</span>
    <span class="nt">&lt;property</span> <span class="na">name=</span><span class="s">"LOG_DIR"</span> <span class="na">value=</span><span class="s">"logs"</span> <span class="nt">/&gt;</span>
    <span class="nt">&lt;appender</span> <span class="na">name=</span><span class="s">"CONSOLE"</span> <span class="na">class=</span><span class="s">"ch.qos.logback.core.ConsoleAppender"</span><span class="nt">&gt;</span>
        <span class="nt">&lt;encoder&gt;</span>
            <span class="nt">&lt;pattern&gt;</span>%green(%d{yyyy-MM-dd HH:mm:ss.SSS}) %magenta([%thread]) %highlight(%5level) %cyan(%logger) - %yellow(%msg%n)<span class="nt">&lt;/pattern&gt;</span>
        <span class="nt">&lt;/encoder&gt;</span>
    <span class="nt">&lt;/appender&gt;</span>
    <span class="nt">&lt;appender</span> <span class="na">name=</span><span class="s">"FILE"</span> <span class="na">class=</span><span class="s">"ch.qos.logback.core.rolling.RollingFileAppender"</span><span class="nt">&gt;</span>
        <span class="nt">&lt;file&gt;</span>${LOG_DIR}/coupon.log<span class="nt">&lt;/file&gt;</span>
        <span class="nt">&lt;encoder&gt;</span>
            <span class="nt">&lt;pattern&gt;</span>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %5level %logger - %msg%n<span class="nt">&lt;/pattern&gt;</span>
        <span class="nt">&lt;/encoder&gt;</span>
        <span class="nt">&lt;rollingPolicy</span> <span class="na">class=</span><span class="s">"ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy"</span><span class="nt">&gt;</span>
            <span class="nt">&lt;fileNamePattern&gt;</span>${LOG_DIR}/coupon-%d{yyyy-MM-dd}.%i.log<span class="nt">&lt;/fileNamePattern&gt;</span>
            <span class="nt">&lt;maxFileSize&gt;</span>30MB<span class="nt">&lt;/maxFileSize&gt;</span>
            <span class="nt">&lt;maxHistory&gt;</span>30<span class="nt">&lt;/maxHistory&gt;</span>
        <span class="nt">&lt;/rollingPolicy&gt;</span>
    <span class="nt">&lt;/appender&gt;</span>

    <span class="nt">&lt;logger</span> <span class="na">name=</span><span class="s">"client.coupon"</span> <span class="na">level=</span><span class="s">"DEBUG"</span><span class="nt">/&gt;</span>
    <span class="nt">&lt;root</span> <span class="na">level=</span><span class="s">"INFO"</span><span class="nt">&gt;</span>
        <span class="nt">&lt;appender-ref</span> <span class="na">ref=</span><span class="s">"CONSOLE"</span> <span class="nt">/&gt;</span>
        <span class="nt">&lt;appender-ref</span> <span class="na">ref=</span><span class="s">"FILE"</span> <span class="nt">/&gt;</span>
    <span class="nt">&lt;/root&gt;</span>
<span class="nt">&lt;/configuration&gt;</span>

</code></pre></div></div>

<h3 id="mdc-verification">MDC verification</h3>

<p>이제는 성능테스트 도구인 vegeta 를 이용해서 트래픽을 발생시켜서 MDC 가 정상적으로 동작하는지 검증하려고 한다!</p>

<p><strong>검증방법</strong></p>

<p>vegeta 로 300tps 를 5분동안 부하 생성</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>GET http://localhost:8081/coupons/1
GET http://localhost:8081/coupons/2
GET http://localhost:8081/coupons/3
GET http://localhost:8081/coupons/4
GET http://localhost:8081/coupons/5
</code></pre></div></div>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>vegeta attack <span class="nt">-duration</span><span class="o">=</span>5m <span class="nt">-rate</span><span class="o">=</span>300 <span class="nt">-targets</span><span class="o">=</span>targets.txt | vegeta report
</code></pre></div></div>

<p>log 파일을 읽어서 API 호출에 대한 정보가 MDC 에 정상적으로 적재되었는지 확인</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">package</span> <span class="nn">client.coupon.config</span><span class="o">;</span>

<span class="kn">import</span> <span class="nn">static</span> <span class="n">org</span><span class="o">.</span><span class="na">junit</span><span class="o">.</span><span class="na">jupiter</span><span class="o">.</span><span class="na">api</span><span class="o">.</span><span class="na">Assertions</span><span class="o">.*;</span>

<span class="kn">import</span> <span class="nn">java.io.BufferedReader</span><span class="o">;</span>
<span class="kn">import</span> <span class="nn">java.io.FileReader</span><span class="o">;</span>
<span class="kn">import</span> <span class="nn">java.io.IOException</span><span class="o">;</span>

<span class="kn">import</span> <span class="nn">org.junit.jupiter.api.Test</span><span class="o">;</span>

<span class="kn">import</span> <span class="nn">lombok.extern.slf4j.Slf4j</span><span class="o">;</span>

<span class="nd">@Slf4j</span>
<span class="kd">class</span> <span class="nc">FeignTraceTest</span> <span class="o">{</span>

	<span class="kd">private</span> <span class="kd">static</span> <span class="kd">final</span> <span class="nc">String</span> <span class="no">PATH</span> <span class="o">=</span> <span class="s">"logs/coupon.log"</span><span class="o">;</span>

	<span class="nd">@Test</span>
	<span class="kt">void</span> <span class="nf">test_trace</span><span class="o">()</span> <span class="o">{</span>
		<span class="k">try</span> <span class="o">(</span><span class="kd">final</span> <span class="nc">BufferedReader</span> <span class="n">br</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">BufferedReader</span><span class="o">(</span><span class="k">new</span> <span class="nc">FileReader</span><span class="o">(</span><span class="no">PATH</span><span class="o">)))</span> <span class="o">{</span>
			<span class="nc">String</span> <span class="n">line</span><span class="o">;</span>
			<span class="k">while</span> <span class="o">((</span><span class="n">line</span> <span class="o">=</span> <span class="n">br</span><span class="o">.</span><span class="na">readLine</span><span class="o">())</span> <span class="o">!=</span> <span class="kc">null</span><span class="o">)</span> <span class="o">{</span>
				<span class="k">if</span> <span class="o">(!</span><span class="n">line</span><span class="o">.</span><span class="na">contains</span><span class="o">(</span><span class="s">"[success]"</span><span class="o">)</span> <span class="o">&amp;&amp;</span> <span class="o">!</span><span class="n">line</span><span class="o">.</span><span class="na">contains</span><span class="o">(</span><span class="s">"[fail]"</span><span class="o">))</span> <span class="o">{</span>
					<span class="k">continue</span><span class="o">;</span>
				<span class="o">}</span>

				<span class="kd">final</span> <span class="nc">String</span> <span class="n">parameter</span> <span class="o">=</span> <span class="n">extract</span><span class="o">(</span><span class="n">line</span><span class="o">.</span><span class="na">split</span><span class="o">(</span><span class="s">","</span><span class="o">)[</span><span class="mi">2</span><span class="o">]);</span>
				<span class="kd">final</span> <span class="nc">String</span> <span class="n">path</span> <span class="o">=</span> <span class="n">extract</span><span class="o">(</span><span class="n">line</span><span class="o">.</span><span class="na">split</span><span class="o">(</span><span class="s">","</span><span class="o">)[</span><span class="mi">1</span><span class="o">]);</span>
				<span class="n">log</span><span class="o">.</span><span class="na">info</span><span class="o">(</span><span class="s">"parameter : {}, path : {}"</span><span class="o">,</span> <span class="n">parameter</span><span class="o">,</span> <span class="n">path</span><span class="o">);</span>
				<span class="n">assertEquals</span><span class="o">(</span><span class="s">"/coupons/"</span> <span class="o">+</span> <span class="n">parameter</span><span class="o">,</span> <span class="n">path</span><span class="o">);</span>
			<span class="o">}</span>
		<span class="o">}</span> <span class="k">catch</span> <span class="o">(</span><span class="nc">IOException</span> <span class="n">e</span><span class="o">)</span> <span class="o">{</span>
			<span class="n">e</span><span class="o">.</span><span class="na">printStackTrace</span><span class="o">();</span>
		<span class="o">}</span>
	<span class="o">}</span>

	<span class="kd">private</span> <span class="nc">String</span> <span class="nf">extract</span><span class="o">(</span><span class="kd">final</span> <span class="nc">String</span> <span class="n">line</span><span class="o">)</span> <span class="o">{</span>
		<span class="k">return</span> <span class="n">line</span><span class="o">.</span><span class="na">replaceAll</span><span class="o">(</span><span class="s">" "</span><span class="o">,</span> <span class="s">""</span><span class="o">).</span><span class="na">split</span><span class="o">(</span><span class="s">":"</span><span class="o">)[</span><span class="mi">1</span><span class="o">];</span>
	<span class="o">}</span>

<span class="o">}</span>
</code></pre></div></div>

<p><strong>검증결과</strong></p>

<p>성공과 실패에 대해서 MDC 를 잘 clear 하고 있으며, 동시성 이슈도 없어보인다! 성공!</p>

<p><img src="https://raw.githubusercontent.com/lkhlkh23/lkhlkh23.github.io/master/images/2024-09-30/0.png" alt="0.png" /></p>

<p>MDC 에 대해서 간단하게 알아보면서 추가적으로 Servlet 에 대해서도 정리를 해봤다.</p>

<h3 id="what-is-servlet-and-servlet-container">What is Servlet and Servlet Container..?</h3>

<p>Servlet 은 Servlet 클래스의 구현 규칙을 바탕으로 사용자의 요청을 처리하는 웹 프로그래밍 기술이다. Java 에서 동적인 웹 페이지를 생성는데 사용하는 기술이다. (예 : PHP)</p>

<p>Tomcat (Web Application Server, WAS) 은 Servlet Container 라고 불리며, Servlet 을 관리하고, <code class="language-plaintext highlighter-rouge">Servlet 생명주기 관리</code> , <code class="language-plaintext highlighter-rouge">통신 지원</code> 그리고 <code class="language-plaintext highlighter-rouge">멀티스레드 지원 역할</code>을 수행한다.</p>

<p><img src="https://raw.githubusercontent.com/lkhlkh23/lkhlkh23.github.io/master/images/2024-09-30/1.png" alt="1.png" /></p>

<p><strong>Servlet 생명주기 관리</strong></p>

<p>Servlet 은 Servlet Container 에 의해 싱글톤으로 관리된다. Servlet Container 가 시작되면 Servlet 클래스를 로딩하고, 인스턴스를 생성하여 <code class="language-plaintext highlighter-rouge">init()</code> 메서드를 호출해 초기화 작업을 수행한다. 그리고 클라이언트의 요청이 들어오면 <code class="language-plaintext highlighter-rouge">service()</code> 메서드를 통해 요청을 처리하며, 요청 방식에 따라 적절한 메서드(<code class="language-plaintext highlighter-rouge">doGet()</code>, <code class="language-plaintext highlighter-rouge">doPost()</code>)를 호출한다.  Container 가 종료될 때는 <code class="language-plaintext highlighter-rouge">destroy()</code> 메서드를 이용해서 자원을 해제하고, GC 를 통해 Servlet 객체가 메모리에서 제거된다.</p>

<p><strong>통신 지원 (HttpServeletRequest 와 HttpServeletResponse 객체 생성)</strong></p>

<p>Servlet 은 클라이언트와 서버 간의 통신을 쉽게 할 수 있도록 HttpServletRequest와 HttpServletResponse 객체를 제공한다. <code class="language-plaintext highlighter-rouge">HttpServletRequest</code> 는 HTTP 요청 정보를 Servlet 에 전달하며, 헤더, 파라미터, 쿠키, URI 등의 데이터를 읽거나 Body의 스트림을 처리할 수 있다. <code class="language-plaintext highlighter-rouge">HttpServletResponse</code> 는 클라이언트에게 응답 코드, 콘텐츠 타입, 메시지 등을 설정하고 전송할 수 있다. 이를 통해 개발자는 통신 관련 세부 사항에 신경 쓰지 않고 비즈니스 로직에 집중할 수 있습니다.</p>

<p><strong>멀티스레드 지원</strong></p>

<p><img src="https://raw.githubusercontent.com/lkhlkh23/lkhlkh23.github.io/master/images/2024-09-30/2.png" alt="2.png" /></p>

<p>Servlet 은 싱글톤으로 관리되며, 여러 요청이 들어와도 하나의 Servlet 인스턴스로 처리된다. Servlet Container 는 요청이 올때마다 새로운 스레드를 생성하고, 요청이 끝나면 스레드를 제거한다. 하지만 요청마다 스레드를 생성하는 것은 성능 저하를 일으킬 수 있기 때문에, <code class="language-plaintext highlighter-rouge">Thread Pool</code> 을 사용한다.</p>

<p>Thrad Pool 은 필요한 스레드를 미리 생성해 두고 관리한다. 요청이 들어오면 Thrad Pool 에서 스레드를 꺼내 사용하고, 사용이 끝나면 다시 반납한다. 만약 Thrad Pool 이 모두 사용 중일 때는, 요청을 거절하거나 일정 수만큼 Queue 에서 대기하도록 설정할 수 있다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">server</span><span class="pi">:</span>
  <span class="na">tomcat</span><span class="pi">:</span>
    <span class="na">accept-count</span><span class="pi">:</span> <span class="s">10</span> 
    <span class="na">max-connections</span><span class="pi">:</span> <span class="m">150</span>
    <span class="na">threads</span><span class="pi">:</span>
      <span class="na">max</span><span class="pi">:</span> <span class="m">50</span> <span class="c1"># default : 200</span>
      <span class="na">min-spare</span><span class="pi">:</span> <span class="m">20</span>
</code></pre></div></div>

<ul>
  <li>server.tomcat.accept-count (default : 100)
    <ul>
      <li>서버가 처리할 수 있는 최대 thread 수 (server.tomcat.max) 를 초과할 경우, Queue 에 적재할 수 있는 Connection 수</li>
    </ul>
  </li>
  <li>server.tomcat.max-connections (default : 8192)
    <ul>
      <li>서버와 클라이언트간의 Connection 수</li>
    </ul>
  </li>
  <li>server.tomcat.thread.max (default : 200)
    <ul>
      <li>동시에 처리될 수 있는 최대 thread 수</li>
    </ul>
  </li>
  <li>server.tomcat.min-spare (default : 10)
    <ul>
      <li>사용하지 않아도 최소로 유지해야하는 thread 수</li>
    </ul>
  </li>
</ul>

<h3 id="how-is-servlet-used-in-springboot">How is Servlet used in SpringBoot..?</h3>

<p>SpringBoot 는 Embedded Tomcat 사용하고 있기 때문에 복잡한 설정 없이 Servlet 을 쉽게 활용할 수 있다. Embedded Tomcat 을 이용해 MVC 패턴 기반의 애플리케이션 개발이 가능해집니다.</p>

<p><img src="https://raw.githubusercontent.com/lkhlkh23/lkhlkh23.github.io/master/images/2024-09-30/3.png" alt="3.png" /></p>

<p>Servlet 은 SpringBoot 의 컨트롤러와 함께 작동하여 클라이언트 요청을 처리하고 비즈니스 로직을 수행한 후 .응답합니다. 또한, @ServletComponentScan 어노테이션을 통해 Servlet 을 등록하고 생명주기를 관리하여 개발자의 편의성을 지원한다.</p>

<h3 id="and-i-am-">And I am …</h3>

<p><img src="https://raw.githubusercontent.com/lkhlkh23/lkhlkh23.github.io/master/images/2024-09-30/4.png" alt="4.png" /></p>

<p>이제 4/4분기만 남았다. 어쩌다가 반칠순이 되었는데, 이러다가 곧 불혹되겠네 …</p>

<p>관리 열심히 해야지 … 물리시간에도 이해못한 가속도의 법칙을 노화를 통해 배우고 있다 …</p>]]></content><author><name>lkhlkh23</name></author><category term="springboot" /><category term="mdc" /><category term="sevelet" /><category term="servlet-container" /><category term="thread-pool" /><summary type="html"><![CDATA[팀원이 외부 API 호출 이력을 남기는 개발을 진행을 하고있는데, MDC 를 이용한다고 들었다. 생소한 단어였다. MDC 활용에 대해 어떤 코멘트도 할 수 없었다. 그래서 정리하려고 한다.]]></summary></entry><entry><title type="html">스프링배치 성능 최적화</title><link href="https://lkhlkh23.github.io/jekyll-theme-yat/springbatch/2024/09/22/spring-batch-performance.html" rel="alternate" type="text/html" title="스프링배치 성능 최적화" /><published>2024-09-22T00:00:00+00:00</published><updated>2024-09-22T00:00:00+00:00</updated><id>https://lkhlkh23.github.io/jekyll-theme-yat/springbatch/2024/09/22/spring-batch-performance</id><content type="html" xml:base="https://lkhlkh23.github.io/jekyll-theme-yat/springbatch/2024/09/22/spring-batch-performance.html"><![CDATA[<p>요즘 시간이 있을 때, 과거 컨퍼런스 영상을 찾아보고 있다. “최신 컨퍼런스 영상도 아닌, 오래된 컨퍼런스 영상을 보고 있냐?!” 라는 질문을 받을 수 있다. 그러나, 역시! 이유가 있다!</p>

<p><img src="https://raw.githubusercontent.com/lkhlkh23/lkhlkh23.github.io/master/images/2024-09-22/0.png" alt="0.png" /></p>

<p>내 업무에 적용해서, 개발뿐만 아니라 운영도 경험할 수 있는, 나에게 스토리를 줄 수 있는 인사이트를 얻고 싶어서이다. 최신 기술들은 현재 조직 환경에는 적합하지 않다. 그래서 과거 컨퍼런스 영상중에서 조직에서 사용하고 있는 기술과 연관된 영상을 우선적으로 찾아보고 있다.</p>

<p>그래서 현 조직에서 잘 사용하고 있는 SpringBatch 와 관련된 강의 <a href="https://www.youtube.com/watch?v=2IIwQDIi3ys">if Kakao 2022 “Batch Performance 극한으로 끌어올리기: 1억 건 데이터 처리를 위한 노력” 강의</a> 를 듣고 내용 정리 및 나의 생각도 같이 담을 예정이다.</p>

<p>SpringBatch 를 개발을 위해서는 두가지 방식이 있다.</p>

<ul>
  <li>Tasklet</li>
  <li>Chunk Oriented Processing</li>
</ul>

<p>Chunk Oriented Processing 에서는 <code class="language-plaintext highlighter-rouge">reader → processor → writer</code> 구조로 처리를 한다. 해당 강의에서는 대용량 데이터를 처리하는 과정에서 각각의 구성요소에서 성능효율을 최대화할 수 있는 방법에 대해 소개하고 있다.</p>

<p><img src="https://raw.githubusercontent.com/lkhlkh23/lkhlkh23.github.io/master/images/2024-09-22/1.png" alt="1.png" /></p>

<p>참고로 Chunk Oriented Processing 이 아래와 같은 절차로 진행을 되고있다고 착각을 했다. 이유는 SpringBatch 4.0.4 버전의 공식문서에서 아래와 같이 나와있었다. 하지만 정확한 절차는 위 그림이다.</p>

<p><img src="https://raw.githubusercontent.com/lkhlkh23/lkhlkh23.github.io/master/images/2024-09-22/2.png" alt="2.png" /></p>

<h3 id="optimizing-reader-performance"><strong>Optimizing Reader Performance</strong></h3>

<p>Reader 성능 최적화를 위해서 <code class="language-plaintext highlighter-rouge">적절한 PaginReader</code> 를 사용하는 전략이 있다.</p>

<p>Chunk Oriented Processing 에서 데이터를 조회하기 위해서 <code class="language-plaintext highlighter-rouge">PagingItemReader</code> 와 <code class="language-plaintext highlighter-rouge">CursorItemReade</code> 를 사용한다.</p>

<p>먼저 <code class="language-plaintext highlighter-rouge">PagingItemReader</code> 는 주로 <code class="language-plaintext highlighter-rouge">JpaPaginItemReader</code> 와 <code class="language-plaintext highlighter-rouge">RepositoryItemReader</code> 구현체를 사용한다. 이 두 가지 Reader는 페이징 방식으로 데이터를 읽어오지만, 데이터 양이 많아질수록 <code class="language-plaintext highlighter-rouge">LIMIT + OFFSET</code> 을 사용하는 쿼리 때문에 성능 저하가 발생할 수 있다.</p>

<blockquote>
  <p><strong>LIMIT + OFFSET 성능 저하 이유</strong></p>

  <p>페이징을 구현할 때 LIMIT 과 OFFSET 을 사용하면, 데이터베이스는 요청된 페이지를 가져오기 전에 이전의 모든 레코드를 스캔한다. 즉, OFFSET 값이 클수록 데이터베이스는 더 많은 데이터를 스캔해야 하고, 이로 인해 성능이 저하된다.</p>

</blockquote>

<p>이를 해결하기 위해서 OFFSET 을 항상 0으로 유지하는, ZERO OFFSET 을 사용한다. <code class="language-plaintext highlighter-rouge">QuerydslZeroOffsetItemReader</code> 를 아래와 같이 커스텀하게 구현할 수 있다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">public</span> <span class="kd">class</span> <span class="nc">QuerydslPagingItemReader</span><span class="o">&lt;</span><span class="no">T</span><span class="o">&gt;</span> <span class="kd">extends</span> <span class="nc">AbstractPagingItemReader</span><span class="o">&lt;</span><span class="no">T</span><span class="o">&gt;</span> <span class="o">{</span>

    <span class="kd">protected</span> <span class="kd">final</span> <span class="nc">Map</span><span class="o">&lt;</span><span class="nc">String</span><span class="o">,</span> <span class="nc">Object</span><span class="o">&gt;</span> <span class="n">jpaPropertyMap</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">HashMap</span><span class="o">&lt;&gt;();</span>
    <span class="kd">protected</span> <span class="nc">EntityManagerFactory</span> <span class="n">entityManagerFactory</span><span class="o">;</span>
    <span class="kd">protected</span> <span class="nc">EntityManager</span> <span class="n">entityManager</span><span class="o">;</span>
    <span class="kd">protected</span> <span class="nc">Function</span><span class="o">&lt;</span><span class="nc">JPAQueryFactory</span><span class="o">,</span> <span class="nc">JPAQuery</span><span class="o">&lt;</span><span class="no">T</span><span class="o">&gt;&gt;</span> <span class="n">queryFunction</span><span class="o">;</span>
    <span class="kd">protected</span> <span class="kt">boolean</span> <span class="n">transacted</span> <span class="o">=</span> <span class="kc">true</span><span class="o">;</span> <span class="c1">// default value</span>

    <span class="kd">protected</span> <span class="nf">QuerydslPagingItemReader</span><span class="o">()</span> <span class="o">{</span>
        <span class="n">setName</span><span class="o">(</span><span class="nc">ClassUtils</span><span class="o">.</span><span class="na">getShortName</span><span class="o">(</span><span class="nc">QuerydslPagingItemReader</span><span class="o">.</span><span class="na">class</span><span class="o">));</span>
    <span class="o">}</span>

    <span class="kd">public</span> <span class="nf">QuerydslPagingItemReader</span><span class="o">(</span><span class="nc">EntityManagerFactory</span> <span class="n">entityManagerFactory</span><span class="o">,</span>
                                    <span class="kt">int</span> <span class="n">pageSize</span><span class="o">,</span>
                                    <span class="nc">Function</span><span class="o">&lt;</span><span class="nc">JPAQueryFactory</span><span class="o">,</span> <span class="nc">JPAQuery</span><span class="o">&lt;</span><span class="no">T</span><span class="o">&gt;&gt;</span> <span class="n">queryFunction</span><span class="o">)</span> <span class="o">{</span>
        <span class="k">this</span><span class="o">(</span><span class="n">entityManagerFactory</span><span class="o">,</span> <span class="n">pageSize</span><span class="o">,</span> <span class="kc">true</span><span class="o">,</span> <span class="n">queryFunction</span><span class="o">);</span>
    <span class="o">}</span>

    <span class="kd">public</span> <span class="nf">QuerydslPagingItemReader</span><span class="o">(</span><span class="nc">EntityManagerFactory</span> <span class="n">entityManagerFactory</span><span class="o">,</span>
                                    <span class="kt">int</span> <span class="n">pageSize</span><span class="o">,</span>
                                    <span class="kt">boolean</span> <span class="n">transacted</span><span class="o">,</span>
                                    <span class="nc">Function</span><span class="o">&lt;</span><span class="nc">JPAQueryFactory</span><span class="o">,</span> <span class="nc">JPAQuery</span><span class="o">&lt;</span><span class="no">T</span><span class="o">&gt;&gt;</span> <span class="n">queryFunction</span><span class="o">)</span> <span class="o">{</span>
        <span class="k">this</span><span class="o">();</span>
        <span class="k">this</span><span class="o">.</span><span class="na">entityManagerFactory</span> <span class="o">=</span> <span class="n">entityManagerFactory</span><span class="o">;</span>
        <span class="k">this</span><span class="o">.</span><span class="na">queryFunction</span> <span class="o">=</span> <span class="n">queryFunction</span><span class="o">;</span>
        <span class="n">setPageSize</span><span class="o">(</span><span class="n">pageSize</span><span class="o">);</span>
        <span class="n">setTransacted</span><span class="o">(</span><span class="n">transacted</span><span class="o">);</span>
    <span class="o">}</span>

    <span class="cm">/**
     * Reader의 트랜잭션격리 옵션 &lt;br/&gt;
     * - false: 격리 시키지 않고, Chunk 트랜잭션에 의존한다 &lt;br/&gt;
     * (hibernate.default_batch_fetch_size 옵션 사용가능) &lt;br/&gt;
     * - true: 격리 시킨다 &lt;br/&gt;
     * (Reader 조회 결과를 삭제하고 다시 조회했을때 삭제된게 반영되고 조회되길 원할때 사용한다.)
     */</span>
    <span class="kd">public</span> <span class="kt">void</span> <span class="nf">setTransacted</span><span class="o">(</span><span class="kt">boolean</span> <span class="n">transacted</span><span class="o">)</span> <span class="o">{</span>
        <span class="k">this</span><span class="o">.</span><span class="na">transacted</span> <span class="o">=</span> <span class="n">transacted</span><span class="o">;</span>
    <span class="o">}</span>

    <span class="nd">@Override</span>
    <span class="kd">protected</span> <span class="kt">void</span> <span class="nf">doOpen</span><span class="o">()</span> <span class="kd">throws</span> <span class="nc">Exception</span> <span class="o">{</span>
        <span class="kd">super</span><span class="o">.</span><span class="na">doOpen</span><span class="o">();</span>

        <span class="n">entityManager</span> <span class="o">=</span> <span class="n">entityManagerFactory</span><span class="o">.</span><span class="na">createEntityManager</span><span class="o">(</span><span class="n">jpaPropertyMap</span><span class="o">);</span>
        <span class="k">if</span> <span class="o">(</span><span class="n">entityManager</span> <span class="o">==</span> <span class="kc">null</span><span class="o">)</span> <span class="o">{</span>
            <span class="k">throw</span> <span class="k">new</span> <span class="nf">DataAccessResourceFailureException</span><span class="o">(</span><span class="s">"Unable to obtain an EntityManager"</span><span class="o">);</span>
        <span class="o">}</span>
    <span class="o">}</span>

    <span class="nd">@Override</span>
    <span class="nd">@SuppressWarnings</span><span class="o">(</span><span class="s">"unchecked"</span><span class="o">)</span>
    <span class="kd">protected</span> <span class="kt">void</span> <span class="nf">doReadPage</span><span class="o">()</span> <span class="o">{</span>
        <span class="nc">EntityTransaction</span> <span class="n">tx</span> <span class="o">=</span> <span class="n">getTxOrNull</span><span class="o">();</span>

        <span class="nc">JPQLQuery</span><span class="o">&lt;</span><span class="no">T</span><span class="o">&gt;</span> <span class="n">query</span> <span class="o">=</span> <span class="n">createQuery</span><span class="o">()</span>
                <span class="o">.</span><span class="na">offset</span><span class="o">(</span><span class="n">getPage</span><span class="o">()</span> <span class="o">*</span> <span class="n">getPageSize</span><span class="o">())</span>
                <span class="o">.</span><span class="na">limit</span><span class="o">(</span><span class="n">getPageSize</span><span class="o">());</span>

        <span class="n">initResults</span><span class="o">();</span>

        <span class="n">fetchQuery</span><span class="o">(</span><span class="n">query</span><span class="o">,</span> <span class="n">tx</span><span class="o">);</span>
    <span class="o">}</span>

    <span class="kd">protected</span> <span class="nc">EntityTransaction</span> <span class="nf">getTxOrNull</span><span class="o">()</span> <span class="o">{</span>
        <span class="k">if</span> <span class="o">(</span><span class="n">transacted</span><span class="o">)</span> <span class="o">{</span>
            <span class="nc">EntityTransaction</span> <span class="n">tx</span> <span class="o">=</span> <span class="n">entityManager</span><span class="o">.</span><span class="na">getTransaction</span><span class="o">();</span>
            <span class="n">tx</span><span class="o">.</span><span class="na">begin</span><span class="o">();</span>

            <span class="n">entityManager</span><span class="o">.</span><span class="na">flush</span><span class="o">();</span>
            <span class="n">entityManager</span><span class="o">.</span><span class="na">clear</span><span class="o">();</span>
            <span class="k">return</span> <span class="n">tx</span><span class="o">;</span>
        <span class="o">}</span>

        <span class="k">return</span> <span class="kc">null</span><span class="o">;</span>
    <span class="o">}</span>

    <span class="kd">protected</span> <span class="nc">JPAQuery</span><span class="o">&lt;</span><span class="no">T</span><span class="o">&gt;</span> <span class="nf">createQuery</span><span class="o">()</span> <span class="o">{</span>
        <span class="nc">JPAQueryFactory</span> <span class="n">queryFactory</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">JPAQueryFactory</span><span class="o">(</span><span class="n">entityManager</span><span class="o">);</span>
        <span class="k">return</span> <span class="n">queryFunction</span><span class="o">.</span><span class="na">apply</span><span class="o">(</span><span class="n">queryFactory</span><span class="o">);</span>
    <span class="o">}</span>

    <span class="kd">protected</span> <span class="kt">void</span> <span class="nf">initResults</span><span class="o">()</span> <span class="o">{</span>
        <span class="k">if</span> <span class="o">(</span><span class="nc">CollectionUtils</span><span class="o">.</span><span class="na">isEmpty</span><span class="o">(</span><span class="n">results</span><span class="o">))</span> <span class="o">{</span>
            <span class="n">results</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">CopyOnWriteArrayList</span><span class="o">&lt;&gt;();</span>
        <span class="o">}</span> <span class="k">else</span> <span class="o">{</span>
            <span class="n">results</span><span class="o">.</span><span class="na">clear</span><span class="o">();</span>
        <span class="o">}</span>
    <span class="o">}</span>

    <span class="cm">/**
     * where 의 조건은 id max/min 을 이용한 제한된 범위를 가지게 한다
     * @param query
     * @param tx
     */</span>
    <span class="kd">protected</span> <span class="kt">void</span> <span class="nf">fetchQuery</span><span class="o">(</span><span class="nc">JPQLQuery</span><span class="o">&lt;</span><span class="no">T</span><span class="o">&gt;</span> <span class="n">query</span><span class="o">,</span> <span class="nc">EntityTransaction</span> <span class="n">tx</span><span class="o">)</span> <span class="o">{</span>
        <span class="k">if</span> <span class="o">(</span><span class="n">transacted</span><span class="o">)</span> <span class="o">{</span>
            <span class="n">results</span><span class="o">.</span><span class="na">addAll</span><span class="o">(</span><span class="n">query</span><span class="o">.</span><span class="na">fetch</span><span class="o">());</span>
            <span class="k">if</span><span class="o">(</span><span class="n">tx</span> <span class="o">!=</span> <span class="kc">null</span><span class="o">)</span> <span class="o">{</span>
                <span class="n">tx</span><span class="o">.</span><span class="na">commit</span><span class="o">();</span>
            <span class="o">}</span>
        <span class="o">}</span> <span class="k">else</span> <span class="o">{</span>
            <span class="nc">List</span><span class="o">&lt;</span><span class="no">T</span><span class="o">&gt;</span> <span class="n">queryResult</span> <span class="o">=</span> <span class="n">query</span><span class="o">.</span><span class="na">fetch</span><span class="o">();</span>
            <span class="k">for</span> <span class="o">(</span><span class="no">T</span> <span class="n">entity</span> <span class="o">:</span> <span class="n">queryResult</span><span class="o">)</span> <span class="o">{</span>
                <span class="n">entityManager</span><span class="o">.</span><span class="na">detach</span><span class="o">(</span><span class="n">entity</span><span class="o">);</span>
                <span class="n">results</span><span class="o">.</span><span class="na">add</span><span class="o">(</span><span class="n">entity</span><span class="o">);</span>
            <span class="o">}</span>
        <span class="o">}</span>
    <span class="o">}</span>

    <span class="nd">@Override</span>
    <span class="kd">protected</span> <span class="kt">void</span> <span class="nf">doJumpToPage</span><span class="o">(</span><span class="kt">int</span> <span class="n">itemIndex</span><span class="o">)</span> <span class="o">{</span>
    <span class="o">}</span>

    <span class="nd">@Override</span>
    <span class="kd">protected</span> <span class="kt">void</span> <span class="nf">doClose</span><span class="o">()</span> <span class="kd">throws</span> <span class="nc">Exception</span> <span class="o">{</span>
        <span class="n">entityManager</span><span class="o">.</span><span class="na">close</span><span class="o">();</span>
        <span class="kd">super</span><span class="o">.</span><span class="na">doClose</span><span class="o">();</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cm">/**
 * 배치의 형태가 삭제/수정으로 인해 조회 결과가 계속 동적으로 변경될때 사용할 Reader
 * 참고: https://jojoldu.tistory.com/337
 *
 */</span>
<span class="kd">public</span> <span class="kd">class</span> <span class="nc">QuerydslZeroPagingItemReader</span><span class="o">&lt;</span><span class="no">T</span><span class="o">&gt;</span> <span class="kd">extends</span> <span class="nc">QuerydslPagingItemReader</span><span class="o">&lt;</span><span class="no">T</span><span class="o">&gt;</span> <span class="o">{</span>

    <span class="kd">public</span> <span class="nf">QuerydslZeroPagingItemReader</span><span class="o">()</span> <span class="o">{</span>
        <span class="kd">super</span><span class="o">();</span>
        <span class="n">setName</span><span class="o">(</span><span class="nc">ClassUtils</span><span class="o">.</span><span class="na">getShortName</span><span class="o">(</span><span class="nc">QuerydslZeroPagingItemReader</span><span class="o">.</span><span class="na">class</span><span class="o">));</span>
    <span class="o">}</span>

    <span class="kd">public</span> <span class="nf">QuerydslZeroPagingItemReader</span><span class="o">(</span><span class="nc">EntityManagerFactory</span> <span class="n">entityManagerFactory</span><span class="o">,</span>
                                        <span class="kt">int</span> <span class="n">pageSize</span><span class="o">,</span>
                                        <span class="nc">Function</span><span class="o">&lt;</span><span class="nc">JPAQueryFactory</span><span class="o">,</span> <span class="nc">JPAQuery</span><span class="o">&lt;</span><span class="no">T</span><span class="o">&gt;&gt;</span> <span class="n">queryFunction</span><span class="o">)</span> <span class="o">{</span>
        <span class="k">this</span><span class="o">();</span>
        <span class="n">setTransacted</span><span class="o">(</span><span class="kc">true</span><span class="o">);</span>
        <span class="kd">super</span><span class="o">.</span><span class="na">entityManagerFactory</span> <span class="o">=</span> <span class="n">entityManagerFactory</span><span class="o">;</span>
        <span class="kd">super</span><span class="o">.</span><span class="na">queryFunction</span> <span class="o">=</span> <span class="n">queryFunction</span><span class="o">;</span>
        <span class="n">setPageSize</span><span class="o">(</span><span class="n">pageSize</span><span class="o">);</span>
    <span class="o">}</span>

    <span class="nd">@Override</span>
    <span class="nd">@SuppressWarnings</span><span class="o">(</span><span class="s">"unchecked"</span><span class="o">)</span>
    <span class="kd">protected</span> <span class="kt">void</span> <span class="nf">doReadPage</span><span class="o">()</span> <span class="o">{</span>

        <span class="nc">EntityTransaction</span> <span class="n">tx</span> <span class="o">=</span> <span class="n">getTxOrNull</span><span class="o">();</span>

        <span class="nc">JPQLQuery</span><span class="o">&lt;</span><span class="no">T</span><span class="o">&gt;</span> <span class="n">query</span> <span class="o">=</span> <span class="n">createQuery</span><span class="o">()</span>
                <span class="o">.</span><span class="na">offset</span><span class="o">(</span><span class="mi">0</span><span class="o">)</span>
                <span class="o">.</span><span class="na">limit</span><span class="o">(</span><span class="n">getPageSize</span><span class="o">());</span>

        <span class="n">initResults</span><span class="o">();</span>

        <span class="n">fetchQuery</span><span class="o">(</span><span class="n">query</span><span class="o">,</span> <span class="n">tx</span><span class="o">);</span>
    <span class="o">}</span>

<span class="o">}</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">CursorItemReader</code> 를 사용한다면, OFFSET 에 의한 성능 저하를 걱정할 필요가 없다.</p>

<p>그러나, 구현체인 <code class="language-plaintext highlighter-rouge">JpaCursorItemReader</code> 는 권장하지 않는다. JpaCursorItemReader 는 데이터를 모두 조회한 후, 애플리케이션에서 직접 Cursor 하는 방식이다. 그렇기 때문에 데이터가 많을 경우 OOM 이 발생할 수 있다.</p>

<p>그러므로, <code class="language-plaintext highlighter-rouge">JdbcCursorItemReader</code> 와 <code class="language-plaintext highlighter-rouge">HibernateCursorItemReader</code> 을 권장한다. 두개의 Reader 는 DB 에서 Cursor 하는 방식이기 때문에 OOM 을 걱정하지 않아도 된다. 그러나, 개인적으로 선호하지 않는다. 이 두 Reader 는 데이터를 조회할 때 HQL이나 Native Query를 사용해야 하기 때문에, 코드의 가독성이 떨어지고, 유지보수가 어렵고, 데이터베이스 종속적이라는 문제가 있다.</p>

<p>영상에서는 JetBrain Kotlin 기반 ORM 프레임워크 <code class="language-plaintext highlighter-rouge">Exposed</code> 를 추천했지만, 오직 Kotlin 만 호환이 되기 때문에 현재 조직에서 사용하는 프로젝트에는 적합하지 않다. 그래서, 나는 <code class="language-plaintext highlighter-rouge">QuerydslZeroOffsetItemReader</code> 를 선호한다.</p>

<h3 id="optimizing-processor-performance"><strong>Optimizing Processor Performance</strong></h3>

<p>Processor 성능 최적화를 위해서 <code class="language-plaintext highlighter-rouge">redis</code> 를 사용하는 전략이 있다.</p>

<p>강의에서는 reader 에서 <code class="language-plaintext highlighter-rouge">GROUP BY + SUM</code> 같은 집계 쿼리를 사용하는 것이 성능 저하의 원인이 될 수 있다고 지적했다. 데이터가 적을 때는 간단하게 처리할 수 있지만, 처리할 데이터가 많아질 경우 비효율적이다. 또한, 쿼리가 복잡해질수록 쿼리 튜닝도 복잡해지고 어려워진다. 이 과정에서 쿼리 성능을 최적화하기 위해 과도한 인덱스를 생성하게 되면, 이는 다시 INSERT 나 UPDATE 성능을 저하시킬 수 있다.</p>

<p>반대로, <code class="language-plaintext highlighter-rouge">GROUP BY + SUM</code> 같은 집계 쿼리없이 전체 데이터를 모두 애플리케이션으로 가져와 처리한다면, 메모리 부족 문제가 발생할 수 있고, 데이터베이스에서 직접 처리하는 것보다 처리 속도도 훨씬 느리다.</p>

<p>이러한 문제를 해결하기 위해 redis 가 사용된다. redis 를 단순히 캐시로만 생각할 수 있지만, 사실 중간 연산을 처리하는 장비로도 활용할 수 있다. redis 는 메모리에서 작업하기 때문에 연산 속도가 매우 빠르고, 메모리 공간도 충분히 확보할 수 있다. 또한, 인메모리 특성상 중간 연산된 데이터가 영구적으로 저장되지 않고 휘발되어 관리가 편하다.</p>

<p>물론, redis 에 데이터를 요청할 때 발생할 수 있는 네트워크 지연이 걱정될 수 있다. 그러나 이를 해결하기 위해 <code class="language-plaintext highlighter-rouge">redis pipeline</code> 을 사용할 수 있다. redis pipeline 을 사용하면 여러 요청을 한 번에 묶어서 처리할 수 있어, 요청 간의 네트워크 지연을 최소화할 수 있다.</p>

<h3 id="optimizing-writer-performance"><strong>Optimizing Writer Performance</strong></h3>

<p>writer 성능 최적화를 위해서는 <code class="language-plaintext highlighter-rouge">명시적 쿼리 사용</code>, <code class="language-plaintext highlighter-rouge">Bulk Insert</code> 전략이 있다. 간단하게 알아보자!</p>

<p><strong>Bulk Insert - 네트워크 지연 최소화</strong></p>

<p>쿼리를 1개씩 DB 에 요청하는 것이 아닌, N개의 쿼리를 한번에 요청함으로써, <code class="language-plaintext highlighter-rouge">네트워크 지연을 최소화</code>한다. 하지만, chunk 크기를 너무 크게 설정하면 메모리 사용량이 급격히 증가할 수 있으므로, 애플리케이션의 메모리와 데이터베이스의 처리 능력을 고려하여 적절한 크기를 설정하는 것이 중요하다.</p>

<p><strong>명시적 쿼리를 사용 - only 필요한 컬럼 Update 처리</strong></p>

<p>불필요한 컬럼을 포함해서 업데이트를 하면 네트워크 전송량이 증가해 대역폭이 증가하고, <code class="language-plaintext highlighter-rouge">네트워크 지연이 더 발생</code>할 수 있다. 그리고, <code class="language-plaintext highlighter-rouge">I/O 비용도 증가</code> 한다.</p>

<p>Update 를 할 때, 데이터는 Lock 이 걸리는데, 불필요한 컬럼을 포함해서 업데이트를 하면 <code class="language-plaintext highlighter-rouge">넓은 범위의 Lock 이 발생</code>할 수 있기 때문에 동시성 처리에 불리할 수 있다.</p>

<p>그러나, 우리에게 너무 친숙한 JPA 는 위 두가지 전략을 지원하지 않기 때문에 SrpingBatch 에는 적합하지 않는다. 하지만, 여기서 JPA 를 조금 아는 사람은 “@DynamicUpdate 를 통해서 명시적 쿼리를 사용할 수 있다!” 라고 질문을 할 수 있다. 하지만, DynamicUpdate 는 동적쿼리를 생성하는 과정에서 성능저하를 발생할 수 있기 때문에 JPA 를 쓰는 것을 권장하지 않는다!</p>

<p>영상의 뒤에서는 Spring Cloud Data Flow 를 활용해서 효율적인 Batch 자원의 활용과 모니터링에 대해 소개하고 있지만, 나는 영상을 끝까지 들을 수 없었다. 조직에서 Spring Cloud Data Flow 를 적용했지만, 내 손으로 제거했다. 아직은 SCDF 를 도입하기에는 시기가 이른것 같다.</p>

<p>이제 알겠냐?! 내가 왜 과거 컨퍼런스 영상만 보고있는지?! 내 선택은 틀리지 않았어!</p>

<p><img src="https://raw.githubusercontent.com/lkhlkh23/lkhlkh23.github.io/master/images/2024-09-22/3.jpeg" alt="3.jpeg" /></p>]]></content><author><name>lkhlkh23</name></author><category term="springbatch" /><category term="springbatch" /><category term="performance" /><category term="kakao-2022" /><summary type="html"><![CDATA[요즘 시간이 있을 때, 과거 컨퍼런스 영상을 찾아보고 있다. “최신 컨퍼런스 영상도 아닌, 오래된 컨퍼런스 영상을 보고 있냐?!” 라는 질문을 받을 수 있다. 그러나, 역시! 이유가 있다!]]></summary></entry><entry><title type="html">성능 테스트 준비/실행</title><link href="https://lkhlkh23.github.io/jekyll-theme-yat/test/2024/09/08/load-test.html" rel="alternate" type="text/html" title="성능 테스트 준비/실행" /><published>2024-09-08T00:00:00+00:00</published><updated>2024-09-08T00:00:00+00:00</updated><id>https://lkhlkh23.github.io/jekyll-theme-yat/test/2024/09/08/load-test</id><content type="html" xml:base="https://lkhlkh23.github.io/jekyll-theme-yat/test/2024/09/08/load-test.html"><![CDATA[<h3 id="what-is-background">What is background..?</h3>

<p><img src="https://raw.githubusercontent.com/lkhlkh23/lkhlkh23.github.io/master/images/2024-09-08/0.png" alt="0.png" /></p>

<p>최근 시스템의 구조가 변경하는 과정에서 이슈가 발생했다. 사전에 서비스 점검을 공지한 후, 서비스를 중지한 상태에서 진행했다. 작업이 완료되고 서비스 오픈을 앞두고 API 의 속도가 많이 감소되었다. 급하게 PostgreSQL 을 Spec Up 해서 다시 원복했다.</p>

<ul>
  <li>용도에 맞는 DB 의 이원화 (Dynamo DB 추가)</li>
  <li>PostgreSQL Spec Down</li>
</ul>

<p>상용과 유사한 환경에서 충분한 성능 테스트없이 진행하지 않는 것이 문제의 원인이라고 생각한다. 그리고 곧, PostgreSQL 의 Spec Down 작업이 진행될 예정이다. 성능 테스트를 어떻게 하면 좋을지에 대해 나의 간단한 생각을 간단하게 정리해보려고 한다. 참고로 나는 QA 도 SRE 도 아닌, B/E Developer 이기 때문에 전문적이지 않다!</p>

<h3 id="how-to-test">How to test..?</h3>

<p>현재 상황을 보면 서비스 중지없이 상용 서비스에서 PostgreSQL Spec Down 진행 후, 간단한 성능 테스트를 수행할 것 같다. 단, 사용자의 유입이 가장 적은 새벽 시간에 작업이 예정될 것 같다. 그래서 서비스의 중지없이 사용자의 서비스 이용 경험에 최대한 영향을 주지 않는 테스트 구조를 고민했다.</p>

<p><strong>상용과 비슷한 환경에서 검증</strong></p>

<p>이전 작업을 교훈삼아 PostgreSQL Spec Down 작업 이전에 상용과 유사한 환경을 구축하고 성능 테스트를 진행하는 것이 좋다고 생각한다. 하지만 리소스를 포함한 다양한 이유로 위와 같이 진행되지 않을 것 같다.</p>

<p><strong>카나리 테스트</strong></p>

<p><img src="https://raw.githubusercontent.com/lkhlkh23/lkhlkh23.github.io/master/images/2024-09-08/1.png" alt="1.png" /></p>

<p>API 가 1EA 이상이기 때문에 1EA 만 Spec Down PostgreSQL 을 이용해서 조회하고, 나머지 API 는 기존 PostgreSQL 을 이용해서 조회하는 구조이다. 앞에서 제시한 방법보다 적은 리소스를 사용할 수 있다. 그리고 부하를 점진적으로 발생시켜서 부하테스트와 내구테스트를 진행할 수 있다고 생각한다. 하지만, replica DB 구축이 되지 않는 구조로 들었던것 같다. 이 방법으로도 진행되지 않을 것 같다.</p>

<p>replica DB 만 구축이 된다면, 카나리 테스트가 최소한의 리소스로 진행할 수 있는 가장 합리적인 방법이라고 생각한다. 만약 둘다 되지 않는다면, 새벽에 정화수 떠놓고 기도하면서 작업할 수 밖에 없을 것 같다.</p>

<p><img src="https://raw.githubusercontent.com/lkhlkh23/lkhlkh23.github.io/master/images/2024-09-08/2.png" alt="2.png" /></p>

<p>그러나 시니어 개발자들이라면 내가 생각하지 못한 많은 아이디어들이 있을 것이라고 생각한다.</p>

<h3 id="what-type-of-test-will-you-conduct">What type of test will you conduct..?</h3>

<p><img src="https://raw.githubusercontent.com/lkhlkh23/lkhlkh23.github.io/master/images/2024-09-08/3.png" alt="3.png" /></p>

<p>위 이미지는 <a href="https://www.youtube.com/watch?v=3cTn53dtzJI&amp;t=527s">“우아한 테크 10분 테코톡”</a> 강의에 있는 이미지이다. 성능 테스트에 대해서 쉽게 잘 정리되있었다. 요즘 친구들은 실력이 많이 좋은 것 같다고 생각한다.</p>

<ul>
  <li><strong>스모크 테스트</strong>
    <ul>
      <li>최소한의 부하를 주어 시스템이 정상적으로 동작하는지 확인</li>
      <li>테스트 스크립트의 정상동작 확인</li>
    </ul>
  </li>
  <li><strong>스파이크 테스트</strong>
    <ul>
      <li>사용량이 급증하는 상항에서 시스템의 내구성과 성능 이슈 확인</li>
    </ul>
  </li>
  <li><strong>부하 테스트</strong>
    <ul>
      <li>묵표값이 해당되는 부하를 견딜 수 있는지 확인</li>
    </ul>
  </li>
  <li><strong>스트레스 테스트</strong>
    <ul>
      <li>시스템의 최대치에 해당되는 부하를 받았을 때, 시스템의 정상 동작 확인</li>
      <li>부하테스트 선진행 필수</li>
    </ul>
  </li>
  <li><strong>내구 테스트 (흡수 테스트)</strong>
    <ul>
      <li>평균 사용률로 일정 부하를 지속적으로 주며, 시스템이 문제되는 지점 확인</li>
    </ul>
  </li>
  <li><strong>중단점 테스트</strong>
    <ul>
      <li>임계지점을 발견하기 위해 점진적으로 증가시켜서 시스템의 한계 지점 확인</li>
    </ul>
  </li>
</ul>

<p>상용 서비스에서 테스트를 진행한다고 가정을 한다면, <code class="language-plaintext highlighter-rouge">스모크 테스트</code>, <code class="language-plaintext highlighter-rouge">부하 테스트</code> 그리고 <code class="language-plaintext highlighter-rouge">내구 테스트</code>만 진행 할 수 있을 것 같다. 그외 다른 테스트는 상용 서비스를 사용하는 사용자의 이용경험에 영향을 줄 수 있을 것 같다.</p>

<h3 id="what-is-the-goal-of-the-test">What is the goal of the test..?</h3>

<p>성능 테스트의 지표로는 <code class="language-plaintext highlighter-rouge">처리량</code>과 <code class="language-plaintext highlighter-rouge">응답시간</code>이 있다. 처리량과 응답시간은 PostgreSQL 의 최근 2주 요청을 기준으로 선정했다. 그리고 대상 API 는 호출양이 많고, 응답시간이 큰 API 를 대상으로만 진행될 예정이다.</p>

<ul>
  <li>처리량
    <ul>
      <li>초당 처리하는 작업의 수 (RPS)</li>
    </ul>
  </li>
  <li>응답시간
    <ul>
      <li>시스템의 요청 처리시간</li>
    </ul>
  </li>
</ul>

<h3 id="lets-perform-the-test"><strong>Let’s perform the test..!</strong></h3>

<p>이번 성능 테스트를 진행할 때, API 의 파라미터를 다양하게 설정해서 진행이 필요하다고 생각한다. 동일한 파라미터에 대한 API 의 호출은 DB 의 캐시 매커니즘으로 인해서 테스트의 신뢰성을 감소시킬 수 있다고 생각한다.</p>

<p>일단 개인적인 연습이기 때문에 테스트 대상 및 목표는 다음과 같다! 그리고 평소 서비스를 이용하는 사용자의 수를 300명이라고 가정했다.</p>

<ul>
  <li>API : GET https://jsonplaceholder.typicode.com/posts/{id}</li>
  <li>처리량 : 300RPS</li>
  <li>응답시간 : 1초</li>
</ul>

<p><code class="language-plaintext highlighter-rouge">nGrinder</code> 를 이용해서 다양한 파라미터의 API 를 호출할 수 있도록 스크립트를 작성해보겠다. <code class="language-plaintext highlighter-rouge">getRandomId()</code>  이용해서 최대한 캐시 매커니즘의 영향을 받지 않도록 작성했다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>import static net.grinder.script.Grinder.grinder
import static org.junit.Assert.<span class="k">*</span>
import static org.hamcrest.Matchers.<span class="k">*</span>
import net.grinder.script.GTest
import net.grinder.script.Grinder
import net.grinder.scriptengine.groovy.junit.GrinderRunner
import net.grinder.scriptengine.groovy.junit.annotation.BeforeProcess
import net.grinder.scriptengine.groovy.junit.annotation.BeforeThread
import org.junit.Before
import org.junit.BeforeClass
import org.junit.Test
import org.junit.runner.RunWith

import java.util.Date
import java.util.List
import java.util.ArrayList
import java.util.Random

import net.grinder.plugin.http.HTTPRequest
import net.grinder.plugin.http.HTTPPluginControl

import HTTPClient.Cookie
import HTTPClient.CookieModule
import HTTPClient.HTTPResponse
import HTTPClient.NVPair

/<span class="k">**</span>
 <span class="k">*</span> A simple example using the HTTP plugin that shows the retrieval of a
 <span class="k">*</span> single page via HTTP.
 <span class="k">*</span>
 <span class="k">*</span> This script is automatically generated by ngrinder.
 <span class="k">*</span>
 <span class="k">*</span> @author admin
 <span class="k">*</span>/
@RunWith<span class="o">(</span>GrinderRunner<span class="o">)</span>
class TestRunner <span class="o">{</span>

	public static GTest <span class="nb">test
	</span>public static HTTPRequest request
	public static NVPair[] headers <span class="o">=</span> <span class="o">[]</span>
	public static NVPair[] params <span class="o">=</span> <span class="o">[]</span>
	public static Cookie[] cookies <span class="o">=</span> <span class="o">[]</span>
	public static Random random <span class="o">=</span> new Random<span class="o">()</span>

	@BeforeProcess
	public static void beforeProcess<span class="o">()</span> <span class="o">{</span>
		HTTPPluginControl.getConnectionDefaults<span class="o">()</span>.timeout <span class="o">=</span> 6000
		<span class="nb">test</span> <span class="o">=</span> new GTest<span class="o">(</span>1, <span class="s2">"jsonplaceholder.typicode.com"</span><span class="o">)</span>
		request <span class="o">=</span> new HTTPRequest<span class="o">()</span>
		grinder.logger.info<span class="o">(</span><span class="s2">"before process."</span><span class="o">)</span><span class="p">;</span>
	<span class="o">}</span>

	@BeforeThread
	public void beforeThread<span class="o">()</span> <span class="o">{</span>
		test.record<span class="o">(</span>this, <span class="s2">"test"</span><span class="o">)</span>
		grinder.statistics.delayReports<span class="o">=</span><span class="nb">true</span><span class="p">;</span>
		grinder.logger.info<span class="o">(</span><span class="s2">"before thread."</span><span class="o">)</span><span class="p">;</span>
	<span class="o">}</span>

	@Before
	public void before<span class="o">()</span> <span class="o">{</span>
		request.setHeaders<span class="o">(</span>headers<span class="o">)</span>
		cookies.each <span class="o">{</span> CookieModule.addCookie<span class="o">(</span>it, HTTPPluginControl.getThreadHTTPClientContext<span class="o">())</span> <span class="o">}</span>
		grinder.logger.info<span class="o">(</span><span class="s2">"before. init headers and cookies"</span><span class="o">)</span><span class="p">;</span>
	<span class="o">}</span>

	@Test
	public void <span class="nb">test</span><span class="o">(){</span>
		HTTPResponse result <span class="o">=</span> request.GET<span class="o">(</span><span class="s2">"http://jsonplaceholder.typicode.com/posts/"</span> + getRandomId<span class="o">()</span>, params<span class="o">)</span>

		<span class="k">if</span> <span class="o">(</span>result.statusCode <span class="o">==</span> 301 <span class="o">||</span> result.statusCode <span class="o">==</span> 302<span class="o">)</span> <span class="o">{</span>
			grinder.logger.warn<span class="o">(</span><span class="s2">"Warning. The response may not be correct. The response code was {}."</span>, result.statusCode<span class="o">)</span><span class="p">;</span>
		<span class="o">}</span> <span class="k">else</span> <span class="o">{</span>
			assertThat<span class="o">(</span>result.statusCode, is<span class="o">(</span>200<span class="o">))</span><span class="p">;</span>
		<span class="o">}</span>
	<span class="o">}</span>
	
	private int getRandomId<span class="o">()</span> <span class="o">{</span>
        <span class="k">return </span>random.nextInt<span class="o">(</span>100<span class="o">)</span> + 1
    <span class="o">}</span>
<span class="o">}</span>

</code></pre></div></div>

<p>nGrinder 에서 제공하는 <code class="language-plaintext highlighter-rouge">Ramb-Up</code> 을 사용해서 점진적으로 가상 사용자를 증가시킬 예정이다.</p>

<p><img src="https://raw.githubusercontent.com/lkhlkh23/lkhlkh23.github.io/master/images/2024-09-08/4.png" alt="4.png" /></p>

<p><img src="https://raw.githubusercontent.com/lkhlkh23/lkhlkh23.github.io/master/images/2024-09-08/5.png" alt="5.png" /></p>

<p>가상 사용자를 조절하면서 테스트를 진행하면 될 것 같다.</p>

<h3 id="i-think-">I think …</h3>

<p>이전 회사에서 서비스 오픈 전 또는 프로모션 전에 성능 테스트를 진행했다. 성능 테스트 요청서만 작성했을 뿐, 성능 테스트에 대한 고민을 해본적이 없었다. 그때 프로세스 및 테스트 방식에 대해 더 많이 관심을 가졌다면, 지금보다 더 좋은 방식을 고안할 수 있었을텐데 …</p>

<p><img src="https://raw.githubusercontent.com/lkhlkh23/lkhlkh23.github.io/master/images/2024-09-08/6.png" alt="6.png" /></p>

<p>이번 성능 테스트를 진행하는 과정에서 (내가 참여하지는 않았지만) 제일 아쉬운점은 테스트의 목표 및 일정에 대해서만 논의가 될뿐, 어떤 시나리오로 테스트를 할지에 대해서는 논의가 되지 않는다는 점이다.</p>

<p>그리고 서비스 중지없이 상용 서비스에서 테스트를 한다는점이 가장 아쉽다.</p>

<p>오늘 노을이 너무 이뻣다. 마치 피로 물든것 마냥 …</p>]]></content><author><name>lkhlkh23</name></author><category term="test" /><category term="test" /><category term="performance" /><summary type="html"><![CDATA[What is background..?]]></summary></entry><entry><title type="html">성능 테스트 도구 nGrinder</title><link href="https://lkhlkh23.github.io/jekyll-theme-yat/test/2024/09/07/ngrinder.html" rel="alternate" type="text/html" title="성능 테스트 도구 nGrinder" /><published>2024-09-07T00:00:00+00:00</published><updated>2024-09-07T00:00:00+00:00</updated><id>https://lkhlkh23.github.io/jekyll-theme-yat/test/2024/09/07/ngrinder</id><content type="html" xml:base="https://lkhlkh23.github.io/jekyll-theme-yat/test/2024/09/07/ngrinder.html"><![CDATA[<p>이전에 <a href="https://lkhlkh23.github.io/test/2024/08/30/vegeta.html">Vegeta</a> 가 조직 내부적으로 잘 이용되었다. 하지만, 아쉬웠다. vegeta 를 제외하고 다양한 성능테스트 도구들이 많은데, 왜 가장 기본적인 기능을 제공해주는 vegeta 가 계속 쓰이는 것일까?! 이러다가 vegeta 가 공식적인 성능 테스트 도구가 되지는 않겠지?! 라는 불안감이 들어서 다른 성능 테스트 도구를 소개해보려고 한다.</p>

<p><img src="https://raw.githubusercontent.com/lkhlkh23/lkhlkh23.github.io/master/images/2024-09-07/0.png" alt="0.png" /></p>
<h3 id="what-is-ngrinder">What is nGrinder..?</h3>

<p><code class="language-plaintext highlighter-rouge">네이버에서 제공</code>하는 <code class="language-plaintext highlighter-rouge">서버 부하 테스트를 위한 오픈 소스 프로젝트</code> 이다.</p>

<ul>
  <li>controller
    <ul>
      <li>테스트 수행을 위한 스크립트를 생성</li>
      <li>성능 측정을 위한 GUI 제공 및 결과 노출</li>
    </ul>
  </li>
  <li>agent
    <ul>
      <li>controller 의 요청을 받아 부하 발생</li>
    </ul>
  </li>
</ul>

<h3 id="what-is-ngrinder-features">What is nGrinder features..?</h3>

<p><strong>무료 + 한국어 + 커뮤니티</strong></p>

<p><a href="https://github.com/naver/ngrinder/wiki">nGrinder</a> 는 <code class="language-plaintext highlighter-rouge">네이버에서 제공하는 서버 부하 테스트를 위한 오픈 소스 프로젝트</code> 라고 정의했다. 네이버에서 제공하기 때문에 한국어가 지원되고,  오픈 소스라서 당연히 무료이다. nGrinder 의 가장 큰 장점이라고 생각한다. 또한, 활발한 커뮤니티가 존재하고 한국어로 지원되는 관련 문서나 튜토리얼을 통해 쉽게 학습할 수 있다.</p>

<p><strong>자동화된 테스트 및 CI/CD 연동</strong></p>

<p>nGrinder 는 Jenkins 와 같은 CI/CD 파이프라인과 연동이 가능하기 때문에 성능 테스트를 자동화할 수 있다. <a href="https://www.youtube.com/watch?v=Ovcy02QFt9c&amp;t=1057s">2021 우아콘의 강의</a>를 보면 우아한형제에서 nGrinder + AWS CLI + Jenkins 를 이용해서 성능 테스트를 자동화한 사례를 볼 수 있다.</p>

<ul>
  <li>nGrinder : 성능 테스트 수행 및 결과 리포트</li>
  <li>AWS CLI : 성능 테스트에 필요한 리소스 생성 및 반납</li>
  <li>Jenkins : 성능 테스트에 수행되는 반복적인 구조를 파이프라인화</li>
</ul>

<p><strong>실시간 모니터링 및 직관적인 리포트 제공</strong></p>

<p>nGrinder 는 테스트 종료 후 다양한 성능 지표를 시각화할 수 있는 많은 레포트를 제공한다. 처음에는 자바 개발자기이기 때문에 자바 기반의 Jmeter 에 많은 관심이 있었지만, 결과 레포트를 보고, nGrinder 를 선택하게 되었다.</p>

<p><strong>스크립트 기반의 유연한 테스트</strong></p>

<p>nGrinder 는 <a href="https://github.com/naver/ngrinder/wiki/Groovy-Script-Structure">Groovy</a> 기반의 스크립트를 사용하기 때문에 유연한 성능 테스트를 지원한다. 테스트 시나리오를 쉽게 정의할 수 있고, HTTP 테스트 이외에도 데이터베이스 쿼리, 파일 전송 등 다양한 형태의 부하 테스트가 가능하다.</p>

<h3 id="what-is-ngrinder-limitation">What is nGrinder limitation..?</h3>

<p><strong>복잡한 스크립트 작성</strong></p>

<p>nGrinder 는 Groovy 기반의 스크립트를 사용하여 테스트 시나리오를 정의하는데, Groovy 에 친숙하지 않는 사용자에게는 다소 어려울 수 있다. Groovy 가 Java 와 많이 유사하기 때문에 대부분의 개발자라면 많이 어렵지 않을 것이다.</p>

<p><strong>메모리와 CPU 사용량 관리</strong></p>

<p>nGrinder 는 대용량 요청에 대한 성능 테스트를 지원할 때, 에이전트의 메모리 및 CPU 사용량이 급격히 증가할 수 있다. 리소스 이슈는 실제 테스트를 방해하는 요소가 될 수 있기 때문에 리소스 관리에 주의가 필요하다.</p>

<p><strong>제한적인 프로토콜</strong></p>

<p>nGrinder는 HTTP(S), TCP, UDP 와 같은 기본적인 프로토콜을 지원하지만, 아래와 같은 프로콜에 대해서는 지원이 한정적이다.</p>

<ul>
  <li>WebSocket</li>
  <li>MQTT</li>
  <li>SFTP</li>
  <li>RabbitMQ/AMQP</li>
  <li>SMPP</li>
</ul>

<h3 id="why-did-i-use-ngrinder">Why did I use nGrinder?</h3>

<p>nGrinder 를 선택한 가장 큰 이유는 한국어와 직관적인 리포트 때문이다. 처음 위에서 언급한것과 같이 자바 기반의 Jmeter 가 1순위였다. 하지만, 레포트를 보고 <code class="language-plaintext highlighter-rouge">Jmeter</code> 의 순위가 뒤로 밀려났다. 다음에는 Grafana 와 호환성이 좋은 <code class="language-plaintext highlighter-rouge">K6</code> 도 후보였지만, 100% 무료가 아니기 때문에 후보에서 탈락했다.</p>

<p>당분간 대용량 요청에 대한 테스트를 진행할 계획이 없고, 대부분 HTTP 기반의 테스트를 수행할 예정이기 때문에 nGrinder 가 가장 적합한 성능 테스트 도구였다.</p>

<h3 id="how-to-use-ngrinder">How to use nGrinder..?</h3>

<p><strong>Docker 를 이용해서 Controller + Agent 설치</strong></p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>version: <span class="s1">'3.8'</span>
services:
  <span class="c"># nGrinder Controller</span>
  controller:
    image: ngrinder/controller:latest
    container_name: ngrinder-controller
    restart: always
    volumes:
      - ~/ngrinder-controller:/opt/ngrinder-controller
    ports:
      - <span class="s2">"80:80"</span>     <span class="c"># Web UI access</span>
      - <span class="s2">"16001:16001"</span>  <span class="c"># Agent communication port</span>
      - <span class="s2">"12000-12009:12000-12009"</span> <span class="c"># Additional communication ports for agents</span>
    networks:
      - ngrinder-net

  <span class="c"># nGrinder Agent</span>
  agent:
    image: ngrinder/agent:latest
    container_name: ngrinder-agent
    restart: always
    environment:
      - <span class="nv">NGRINDER_CONTROLLER</span><span class="o">=</span>controller:16001
    depends_on:
      - controller
    networks:
      - ngrinder-net

networks:
  ngrinder-net:
    driver: bridge
</code></pre></div></div>

<p><strong>nGrinder 실행</strong></p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># docker 실행 확인</span>
docker ps

<span class="c"># nGrinder 실행</span>
docker-compose up
</code></pre></div></div>

<p><img src="https://raw.githubusercontent.com/lkhlkh23/lkhlkh23.github.io/master/images/2024-09-07/1.png" alt="1.png" /></p>
<ul>
  <li>url : http://localhost/</li>
  <li>id : admin</li>
  <li>password : admin</li>
</ul>

<p><strong>메인 화면 소개</strong></p>

<p><img src="https://raw.githubusercontent.com/lkhlkh23/lkhlkh23.github.io/master/images/2024-09-07/2.png" alt="2.png" /></p>

<p><code class="language-plaintext highlighter-rouge">성능 테스트</code> 는 테스트 결과를 조회하기 위한 화면이고, <code class="language-plaintext highlighter-rouge">스크립트</code> 는 테스트 방법 및 시나리오를 정의하는 화면이다. 또한, <code class="language-plaintext highlighter-rouge">Jython</code> 도 지원을 하는데, 자바 위에서 동작하는 Python 이라고 간단하게 이해하면된다.</p>

<p><strong>성능테스트 화면 소개</strong></p>

<p><img src="https://raw.githubusercontent.com/lkhlkh23/lkhlkh23.github.io/master/images/2024-09-07/3.png" alt="3.png" /></p>

<p><strong>에이전트</strong></p>

<p>테스트에 사용할 에이전트의 수를 정의할 수 있다. 기본은 1개이고 docker 를 통해 에이전트 수를 조정할 수 있다. 에이전트별 가상 사용자의 제한이 있기 때문에 에이전트의 수가 증가할 수록 더 많은 가상 사용자를 대상으로 성능 테스트를 지원할 수 있다. 또한, 에이전트별로 다른 시나리오로 병렬적으로 테스트가 가능하다.</p>

<p><strong>프로레스/스레드 (에이전트별 가상 사용자 = 프로레스 x 스레드)</strong></p>

<p><code class="language-plaintext highlighter-rouge">프로세스</code>는 OS 에서 독립적인 작업의 단위이기 때문에 프로세스가 각각 독립적인 자원을 가지고 있다. nGrinder 는 N개의 프로세스를 가질 수 있으며, 1개의 프로세스는 N개의 스레드를 가질 수 있다.</p>

<p><code class="language-plaintext highlighter-rouge">스레드</code>는 하나의 프로세스에서 실행되는 작업의 단위이고, 실제로 부하를 생성하는 가상의 사용자 역할을 한다. 스레드는 프로세스 내의 자원을 공유해서 사용한다.</p>

<p>nGrinder 는 메모리와 CPU 사용량 관리가 필요하다는 단점을 가지고 있기 때문에 프로세스와 스레드의 수를 적절하게 조정할 필요가 있다.</p>

<p><strong>Ramp-Up 사용</strong></p>

<p><code class="language-plaintext highlighter-rouge">Ramp-Up</code> 은 점진적으로 가상의 사용자를 증가시킬 수 있는 기능을 제공한다.</p>

<ul>
  <li>초기 개수 : 처음 시작하는 프로세스의 수</li>
  <li>Ramp-Up 주기 : 증가하는 주기</li>
  <li>증가 단위 : Ramp-Up 주기에 따라 증가하는 프로세스의 수</li>
</ul>

<p>예를들어, 아래와 같이 설정했다고 한다면 …</p>

<ul>
  <li>프로세스 : 10</li>
  <li>스레드 : 30</li>
  <li>초기 개수 : 4</li>
  <li>증가 단위 : 2</li>
  <li>초기 대기시간 : 2000</li>
  <li>Ramp-Up 주기 : 1000</li>
</ul>

<p>2초 (초기 대기시간) 후 테스트가 진행될 예정이고, 2초에 120 (초기 개수 x 스레드) 명의 가상 사용자로 테스트가 진행된다. 그리고 1초 (Ramp-Up) 마다 60 (증가 단위 x 스레드) 명의 가상의 사용자가 추가 증가되서 테스트가 진행된다.</p>

<p><strong>테스트 기간/실행 횟수</strong></p>

<p>테스트기간은 테스트 수행시간을 의미하고, 실행 횟수는 스레드당 몇번 반복을 할지를 결정한다. 실행 횟수는 최대 10,000 으로 제한되있다.</p>

<p><strong>스크립트</strong></p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>import static net.grinder.script.Grinder.grinder
import static org.junit.Assert.<span class="k">*</span>
import static org.hamcrest.Matchers.<span class="k">*</span>
import net.grinder.script.GTest
import net.grinder.script.Grinder
import net.grinder.scriptengine.groovy.junit.GrinderRunner
import net.grinder.scriptengine.groovy.junit.annotation.BeforeProcess
import net.grinder.scriptengine.groovy.junit.annotation.BeforeThread
// import static net.grinder.util.GrinderUtils.<span class="k">*</span> // You can use this <span class="k">if </span>you<span class="s1">'re using nGrinder after 3.2.3
import org.junit.Before
import org.junit.BeforeClass
import org.junit.Test
import org.junit.runner.RunWith

import java.util.Date
import java.util.List
import java.util.ArrayList

import net.grinder.plugin.http.HTTPRequest
import net.grinder.plugin.http.HTTPPluginControl

import HTTPClient.Cookie
import HTTPClient.CookieModule
import HTTPClient.HTTPResponse
import HTTPClient.NVPair

// Uncomment this to use new experimental HTTP client.
// import org.ngrinder.http.HTTPRequest
// import org.ngrinder.http.HTTPResponse
// import org.ngrinder.http.cookie.Cookie
// import org.ngrinder.http.cookie.CookieManager

/**
 * A simple example using the HTTP plugin that shows the retrieval of a
 * single page via HTTP.
 *
 * This script is automatically generated by ngrinder.
 *
 * @author admin
 */
@RunWith(GrinderRunner)
class TestRunner {

	public static GTest test
	public static HTTPRequest request
	public static NVPair[] headers = []
	public static NVPair[] params = []
	public static Cookie[] cookies = []

	@BeforeProcess
	public static void beforeProcess() {
		HTTPPluginControl.getConnectionDefaults().timeout = 6000
		test = new GTest(1, "www.naver.com")
		request = new HTTPRequest()
		grinder.logger.info("before process.");
	}

	@BeforeThread
	public void beforeThread() {
		test.record(this, "test")
		grinder.statistics.delayReports=true;
		grinder.logger.info("before thread.");
	}

	@Before
	public void before() {
		request.setHeaders(headers)
		cookies.each { CookieModule.addCookie(it, HTTPPluginControl.getThreadHTTPClientContext()) }
		grinder.logger.info("before. init headers and cookies");
	}

	@Test
	public void test(){
		HTTPResponse result = request.GET("http://www.naver.com", params)

		if (result.statusCode == 301 || result.statusCode == 302) {
			grinder.logger.warn("Warning. The response may not be correct. The response code was {}.", result.statusCode);
		} else {
			assertThat(result.statusCode, is(200));
		}
	}
}

</span></code></pre></div></div>

<p><strong>성능 테스트 결과 - 상세 레포트</strong></p>

<p><img src="https://raw.githubusercontent.com/lkhlkh23/lkhlkh23.github.io/master/images/2024-09-07/4.png" alt="4.png" /></p>

<p><img src="https://raw.githubusercontent.com/lkhlkh23/lkhlkh23.github.io/master/images/2024-09-07/5.png" alt="5.png" /></p>

<ul>
  <li>TPS
    <ul>
      <li>평균 TPS</li>
    </ul>
  </li>
  <li>평균 테스트 시간
    <ul>
      <li>요청 하나를 처리하는데 소요되는 시간</li>
    </ul>
  </li>
  <li>총 실행 테스트
    <ul>
      <li>프로세스 x 스레드 x 테스트 횟수 == 총 Vuser == 총 실행 테스트</li>
    </ul>
  </li>
</ul>

<p><strong>성능 테스트 결과 - CSV</strong></p>

<p>성능 테스트 결과를 CSV 파일로 내려받을 수 있는데, 여러 지표를 확인할 수 있다. 여기에서 이해하기 어려웠던 부분은 <code class="language-plaintext highlighter-rouge">Mean Test Time</code> 과 <code class="language-plaintext highlighter-rouge">Mean Time To First Byte</code> 이었다.</p>

<blockquote>
  <p>Test Time : 클라이언트가 요청을 보낸 시점부터 응답이 완전히 도착할 때까지의 시간</p>

  <p>Mean Time First Byte : 클라이언트가 요청을 보낸 후, 서버가 첫 번째 바이트를 클라이언트에게 전송하기까지 걸린 시간</p>

</blockquote>

<ul>
  <li>Tests
    <ul>
      <li>실행된 테스트의 수</li>
    </ul>
  </li>
  <li>Mean Test Time
    <ul>
      <li>API 호출 또는 트랜잭션이 처리되는 데 걸린 평균 시간</li>
    </ul>
  </li>
  <li>Test Time Standard Deviation
    <ul>
      <li>표준 편차</li>
    </ul>
  </li>
  <li>Mean Response Length
    <ul>
      <li>요청에 대한 응답 데이터의 평균 크기</li>
    </ul>
  </li>
  <li>Response Bytes Per Second
    <ul>
      <li>초당 처리하는 응답 데이터의 양</li>
    </ul>
  </li>
  <li>Mean Time To Resolve Host
    <ul>
      <li>호스트 이름을 IP 주소로 변환하는 데 걸리는 평균 시간 (DNS 해석 시간)</li>
    </ul>
  </li>
  <li>Mean Time To Establish Connection
    <ul>
      <li>서버와의 연결을 설정하는 데 걸리는 평균 시간 (TCP 연결 설정 시간)</li>
    </ul>
  </li>
  <li>Mean Time To First Byte
    <ul>
      <li>요청을 보낸 후 첫 번째 바이트가 수신되기까지 걸린 평균 시간 (서버 응답의 시작 지점)</li>
    </ul>
  </li>
</ul>

<p>이번에는 nGrinder 에 대해 간단하게 알아봤다. 직접 사용하면서 nGrinder 에 치명적인 단점을 찾았다.</p>

<p><img src="https://raw.githubusercontent.com/lkhlkh23/lkhlkh23.github.io/master/images/2024-09-07/6.png" alt="6.png" /></p>

<p>localhost API 의 테스트가 불가능하다는점이다. 이유는 nGrinder 의 에이전트가 127.0.0.1 로 동작하고 있기 때문에 localhost API 의 테스트가 불가능하다는점이다.</p>

<p>세살 버릇 여든까지 간다는 속담이 있다. 초등학교때 방학숙제를 밀려서 늘 급하게 일기를 썻었다. 이전 일기예보를 찾아가면서 힘들게 썻던 기억이 난다. 지금고 그러고 있다. 올해 계획했던 것들을 완료하지 못해서 지금 촉박하게 진행하고 있다. 어른들말 틀린것 하나 없다.</p>]]></content><author><name>lkhlkh23</name></author><category term="test" /><category term="test" /><category term="performance" /><summary type="html"><![CDATA[이전에 Vegeta 가 조직 내부적으로 잘 이용되었다. 하지만, 아쉬웠다. vegeta 를 제외하고 다양한 성능테스트 도구들이 많은데, 왜 가장 기본적인 기능을 제공해주는 vegeta 가 계속 쓰이는 것일까?! 이러다가 vegeta 가 공식적인 성능 테스트 도구가 되지는 않겠지?! 라는 불안감이 들어서 다른 성능 테스트 도구를 소개해보려고 한다.]]></summary></entry><entry><title type="html">SonarQube + Jenkins + Github 연동 (with PR Decoration)</title><link href="https://lkhlkh23.github.io/jekyll-theme-yat/sonarqube/2024/08/31/sonarqube-jenkins-github.html" rel="alternate" type="text/html" title="SonarQube + Jenkins + Github 연동 (with PR Decoration)" /><published>2024-08-31T00:00:00+00:00</published><updated>2024-08-31T00:00:00+00:00</updated><id>https://lkhlkh23.github.io/jekyll-theme-yat/sonarqube/2024/08/31/sonarqube-jenkins-github</id><content type="html" xml:base="https://lkhlkh23.github.io/jekyll-theme-yat/sonarqube/2024/08/31/sonarqube-jenkins-github.html"><![CDATA[<p><a href="https://lkhlkh23.github.io/sonarqube/2024/06/23/sonarqube-github.html">이전 포스팅</a>에서 <code class="language-plaintext highlighter-rouge">soanrqube + github action + github</code> 구조를 이용해서 pull request 를 했을 때, 정적분석 결과를 pull request 에 decoration 을 하는 작업을 정리했다.</p>

<p>폐쇠망에서 github enterprise 를 사용하는 조직이기 때문에 네트워크 방화벽으로 인해서 github action 과  통신할 수 없었다. 그래서 내부의 jenkins 를 활용할 수 밖에 없었다. 그러나 내부의 jenkins 도 여러 네트워크 이슈로 머가리가 많이 아팠다.</p>

<p><img src="https://raw.githubusercontent.com/lkhlkh23/lkhlkh23.github.io/master/images/2024-08-31/0.png" alt="0.png" /></p>

<p>그래서 이번에는 빌드 도구를 github action 이 아닌, jenkins 를 활용하려고 한다. 결국은, <code class="language-plaintext highlighter-rouge">soanrqube + jenkins + github</code>  구조로 정적분석 결과를 pull request 에 decoration 을 하는 작업을 포스팅하겠다.</p>

<h3 id="sonarqube-flow">sonarqube flow</h3>

<p><img src="https://raw.githubusercontent.com/lkhlkh23/lkhlkh23.github.io/master/images/2024-08-31/1.png" alt="1.png" /></p>

<ol>
  <li><code class="language-plaintext highlighter-rouge">github</code> 에서 event 가 발생하면, <code class="language-plaintext highlighter-rouge">webhook</code> 를 통해 jenkins 호출</li>
  <li><code class="language-plaintext highlighter-rouge">jenkins</code> 는 트리거에 의해 설정한 job 실행</li>
  <li><code class="language-plaintext highlighter-rouge">jenkins job</code> 에서 <code class="language-plaintext highlighter-rouge">github checkout → build → sonarqube 정적분석 요청</code>  순서로 실행</li>
  <li><code class="language-plaintext highlighter-rouge">soanrqube</code> 에서 정적분석 완료 후, 결과를 github 에 전달</li>
</ol>

<h3 id="step1-github-setting">Step.1 github setting</h3>

<p><img src="https://raw.githubusercontent.com/lkhlkh23/lkhlkh23.github.io/master/images/2024-08-31/2.png" alt="2.png" /></p>

<p><code class="language-plaintext highlighter-rouge">github project → settings → Hooks → Add webhook</code> 를 클릭해서 위와 같은 화면으로 진입</p>

<ul>
  <li>Payload URL 입력
    <ul>
      <li>http:// <code class="language-plaintext highlighter-rouge">sonarqube-url</code> /generic-webhook-trigger/invoke?token= <code class="language-plaintext highlighter-rouge">token</code>
        <ul>
          <li>jenkins 에서 generic webhook trigger 플러그인을 이용해서 github 의 webhook 를 감지</li>
          <li>token 은 webhook 발생 시, jenkins 의 job 중에서 실행할 job 을 식별하기 위한 고유값</li>
        </ul>
      </li>
    </ul>
  </li>
  <li>Content type 선택
    <ul>
      <li>application/json</li>
    </ul>
  </li>
  <li>Let me select individual events 선택
    <ul>
      <li>Pull requests, Pushes 선택
        <ul>
          <li>webhook 를 발생시킬 이벤트 선택</li>
        </ul>
      </li>
    </ul>
  </li>
  <li>Add webhook</li>
</ul>

<p><img src="https://raw.githubusercontent.com/lkhlkh23/lkhlkh23.github.io/master/images/2024-08-31/3.png" alt="3.png" /></p>

<p>이벤트 발생으로 인한 webhook 의 결과를 <code class="language-plaintext highlighter-rouge">Webhooks → Manage webhook</code> 에서 확인이 가능하다. Payload 를 보면 jenkins 에 이벤트와 관련된 정보를 전달하고 있다.</p>

<ul>
  <li>ref : push 당하는 브랜치 (pull request 이벤트 시, payload (X))</li>
  <li>pull_request.head.ref : pull request 요청 브랜치</li>
  <li>pull_request.number : pull request 번호</li>
  <li>pull_request.base.ref : pull request 에서 merge 당할 브랜치</li>
</ul>

<h3 id="jenkins-setting">jenkins setting</h3>

<p><img src="https://raw.githubusercontent.com/lkhlkh23/lkhlkh23.github.io/master/images/2024-08-31/4.png" alt="4.png" /></p>

<p>github 에서 이벤트로 발생한 webhook 에 의해 jenkins 에서 job 을 실행하기 위해서는 플러그인이 필요하다.</p>

<p><code class="language-plaintext highlighter-rouge">Dashboard → Manage Jenkins → Plugins → Available plugins</code> 메뉴에서 아래 2개의 플러그인을 설치하자.</p>

<ul>
  <li>Generic Webhook Trigger</li>
  <li>Github</li>
</ul>

<p><img src="https://raw.githubusercontent.com/lkhlkh23/lkhlkh23.github.io/master/images/2024-08-31/5.png" alt="5.png" /></p>

<p>다음으로는 job 을 생성하자! <code class="language-plaintext highlighter-rouge">Dashboard → New Item</code> 을 클릭한다! job 의 이름을 입력하고, <code class="language-plaintext highlighter-rouge">Pipeline</code> 을 선택후, 생성한다.</p>

<p><img src="https://raw.githubusercontent.com/lkhlkh23/lkhlkh23.github.io/master/images/2024-08-31/6.png" alt="6.png" /></p>

<ul>
  <li>Description
    <ul>
      <li>job 에 대한 설명을 간략하게 입력</li>
    </ul>
  </li>
  <li>Github project
    <ul>
      <li>github repository url 입력 (예 : https://github.com/lkhlkh23/jenkins-webhook.git)</li>
    </ul>
  </li>
</ul>

<p><img src="https://raw.githubusercontent.com/lkhlkh23/lkhlkh23.github.io/master/images/2024-08-31/7.png" alt="7.png" /></p>

<p>Build Trigger 에서 <code class="language-plaintext highlighter-rouge">Generic Webhook Trigger</code> 선택한다.</p>

<p><img src="https://raw.githubusercontent.com/lkhlkh23/lkhlkh23.github.io/master/images/2024-08-31/8.png" alt="8.png" /></p>

<p>Post content parameter 에서 정의한 파라미터들은 Pipeline 스크립트에서 사용할 예정이다. 여기서 정의하는 파라미터의 값은 <code class="language-plaintext highlighter-rouge">Webhook Payload</code> 이다. 아래 4개의 파라미터를 정의하자!</p>

<ul>
  <li>push 와 pull request 를 구분하기 위한 파라미터
    <ul>
      <li>Variable (Pipeline 스크립트에서 식별하기 위한 변수명)
        <ul>
          <li>ref</li>
        </ul>
      </li>
      <li>Expression
        <ul>
          <li>$.ref</li>
        </ul>
      </li>
      <li>Default value
        <ul>
          <li>none</li>
        </ul>
      </li>
    </ul>
  </li>
  <li>pull request 를 요청하는 브랜치
    <ul>
      <li>Variable
        <ul>
          <li>prBranch</li>
        </ul>
      </li>
      <li>Expression
        <ul>
          <li>$pull_request.head.ref</li>
        </ul>
      </li>
    </ul>
  </li>
  <li>pull request 의 번호
    <ul>
      <li>Variable
        <ul>
          <li>pullRequestNo</li>
        </ul>
      </li>
      <li>Expression
        <ul>
          <li>$pull_request.number</li>
        </ul>
      </li>
    </ul>
  </li>
  <li>pull request 에서 merge 당할 브랜치
    <ul>
      <li>Variable
        <ul>
          <li>prTarger</li>
        </ul>
      </li>
      <li>Expression
        <ul>
          <li>$pull_request.base.ref</li>
        </ul>
      </li>
    </ul>
  </li>
</ul>

<p><img src="https://raw.githubusercontent.com/lkhlkh23/lkhlkh23.github.io/master/images/2024-08-31/9.png" alt="9.png" /></p>

<p>Token 에는 github 의 <code class="language-plaintext highlighter-rouge">Payload URL 에 정의한 token</code> 의 값을 입력하면 된다. token 은 많은 Jenkins 의 job 중에서 webhook 의해서 트리거될 job 을 식별하기 위해 필요하다.</p>

<p>마지막으로는 webhook 에 의해 job 이 트리거되었을 때, 실행할 Pipeline 스크립트를 작성하면 된다. 코드는 아래와 같다</p>

<div class="language-groovy highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">pipeline</span> <span class="o">{</span>
    <span class="n">agent</span> <span class="n">any</span>
    <span class="n">environment</span> <span class="o">{</span>
        <span class="n">JAVA_HOME</span> <span class="o">=</span> <span class="s1">'/usr/lib/jvm/java-21-amazon-corretto.x86_64'</span>
        <span class="n">PATH</span> <span class="o">=</span> <span class="s2">"${env.JAVA_HOME}/bin:${env.PATH}"</span>
    <span class="o">}</span>
    <span class="n">stages</span> <span class="o">{</span>
        <span class="n">stage</span><span class="o">(</span><span class="s1">'Checkout'</span><span class="o">)</span> <span class="o">{</span>
            <span class="n">steps</span> <span class="o">{</span>
                <span class="n">script</span> <span class="o">{</span>
                    <span class="kt">def</span> <span class="n">branch</span> <span class="o">=</span> <span class="n">getBranch</span><span class="o">()</span>
                    <span class="n">echo</span> <span class="s2">"Checking out branch: $branch"</span>
                    <span class="n">git</span> <span class="nl">branch:</span> <span class="n">branch</span><span class="o">,</span> <span class="nl">credentialsId:</span> <span class="s1">'github-credentialsId'</span><span class="o">,</span> <span class="nl">url:</span> <span class="s1">'https://github.enterprise.com/platform-dev/eplat-authentication-api.git'</span>
                <span class="o">}</span>
            <span class="o">}</span>
        <span class="o">}</span>
        <span class="n">stage</span><span class="o">(</span><span class="s1">'Build'</span><span class="o">)</span> <span class="o">{</span>
            <span class="n">steps</span> <span class="o">{</span>
                <span class="n">sh</span> <span class="s1">'''
                    ./gradlew clean
                    ./gradlew build -x test --refresh-dependencies
                '''</span>
            <span class="o">}</span>
        <span class="o">}</span>
        <span class="n">stage</span><span class="o">(</span><span class="s1">'SonarQube analysis'</span><span class="o">)</span> <span class="o">{</span>
            <span class="n">steps</span> <span class="o">{</span>
                <span class="n">script</span> <span class="o">{</span>
                    <span class="kt">def</span> <span class="n">branch</span> <span class="o">=</span> <span class="n">getBranch</span><span class="o">()</span>
                    <span class="kt">def</span> <span class="n">parameters</span> <span class="o">=</span> <span class="n">getSonarQubeParameters</span><span class="o">(</span><span class="n">branch</span><span class="o">)</span>
                    <span class="n">echo</span> <span class="s2">"$parameters"</span>

                    <span class="n">sh</span> <span class="s2">"""
                        ./gradlew sonar \
                          -Dsonar.token=squ_314f69a9bbd43cda678aeb2e19xxxxxxxxxxx \
                          -Dsonar.projectKey=platform-dev_eplat-authentication-api_xxxx \
                          -Dsonar.projectName=eplat-authentication-api \
                          -Dsonar.host.url=http://10.47.198.116:9000/sonar \
                          -Dsonar.sources=src/main/java \
                          ${parameters} \
                          -Dsonar.java.binaries=build/classes/java/main
                    """</span>
                <span class="o">}</span>
            <span class="o">}</span>
        <span class="o">}</span>
    <span class="o">}</span>
<span class="o">}</span>

<span class="kt">def</span> <span class="nf">getBranch</span><span class="o">()</span> <span class="o">{</span>
    <span class="k">return</span> <span class="o">(</span><span class="s2">"$ref"</span> <span class="o">==</span> <span class="s1">'none'</span><span class="o">)</span> <span class="o">?</span> <span class="s2">"$prBranch"</span> <span class="o">:</span> <span class="s2">"$ref"</span><span class="o">.</span><span class="na">split</span><span class="o">(</span><span class="s1">'refs/heads/'</span><span class="o">)[</span><span class="mi">1</span><span class="o">]</span>
<span class="o">}</span>

<span class="kt">def</span> <span class="nf">getSonarQubeParameters</span><span class="o">(</span><span class="n">branch</span><span class="o">)</span> <span class="o">{</span>
    <span class="kt">def</span> <span class="n">parameters</span> <span class="o">=</span> <span class="s2">""</span>
    <span class="k">if</span> <span class="o">(</span><span class="s2">"$ref"</span> <span class="o">==</span> <span class="s1">'none'</span><span class="o">)</span> <span class="o">{</span>
        <span class="n">parameters</span> <span class="o">+=</span> <span class="s2">"-Dsonar.pullrequest.key=${pullRequestNo} "</span>
        <span class="n">parameters</span> <span class="o">+=</span> <span class="s2">"-Dsonar.pullrequest.branch=${branch} "</span>
        <span class="n">parameters</span> <span class="o">+=</span> <span class="s2">"-Dsonar.pullrequest.base=${prTarget} "</span>
    <span class="o">}</span> <span class="k">else</span> <span class="o">{</span>
        <span class="n">parameters</span> <span class="o">+=</span> <span class="s2">"-Dsonar.branch.name=${branch} "</span>
    <span class="o">}</span>
    <span class="k">return</span> <span class="n">parameters</span>
<span class="o">}</span>

</code></pre></div></div>

<p>Pipeline 스크립트는 3단계가 순차적으로 진행된다.</p>

<ul>
  <li>stage(’Checkout’)
    <ul>
      <li>
        <p>github repository 에서 브랜치의 코드 checkout</p>

        <div class="language-groovy highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">git</span> <span class="nl">branch:</span> <span class="n">branch</span><span class="o">,</span> <span class="nl">credentialsId:</span> <span class="s1">'github-credentialsId'</span><span class="o">,</span> <span class="nl">url:</span> <span class="s1">'https://github.enterprise.com/platform-dev/eplat-authentication-api.git'</span>
</code></pre></div>        </div>
      </li>
      <li>credentialsId
        <ul>
          <li>github 에서 생성한 token
            <ul>
              <li><code class="language-plaintext highlighter-rouge">github → Settings → Developer settings → Personal access tokens → Generate new token</code> 클릭</li>
              <li>scope
                <ul>
                  <li>repo 전체 선택</li>
                  <li>admin:repo_hook 전체 선택</li>
                </ul>
              </li>
            </ul>
          </li>
        </ul>
      </li>
      <li>url
        <ul>
          <li>checkout 할 github url</li>
        </ul>
      </li>
    </ul>
  </li>
  <li>stage(’Build’)
    <ul>
      <li>checkout 한 코드 빌드</li>
    </ul>
  </li>
  <li>stage(’Sonarqube analysis)
    <ul>
      <li>
        <p>빌드한 코드를 Sonarqube 에게 정적분석 의뢰</p>

        <div class="language-groovy highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">script</span> <span class="o">{</span>
    <span class="kt">def</span> <span class="n">branch</span> <span class="o">=</span> <span class="n">getBranch</span><span class="o">()</span>
    <span class="kt">def</span> <span class="n">parameters</span> <span class="o">=</span> <span class="n">getSonarQubeParameters</span><span class="o">(</span><span class="n">branch</span><span class="o">)</span>
    <span class="n">echo</span> <span class="s2">"$parameters"</span>
    
    <span class="n">sh</span> <span class="s2">"""
        ./gradlew sonar \
          -Dsonar.token=squ_314f69a9bbd43cda678aeb2e19xxxxxxxxxxx \
          -Dsonar.projectKey=platform-dev_eplat-authentication-api_xxxx \
          -Dsonar.projectName=eplat-authentication-api \
          -Dsonar.host.url=http://10.47.198.116:9000/sonar \
          -Dsonar.sources=src/main/java \
          ${parameters} \
          -Dsonar.java.binaries=build/classes/java/main
    """</span>
<span class="o">}</span>
      
<span class="kt">def</span> <span class="nf">getBranch</span><span class="o">()</span> <span class="o">{</span>
    <span class="k">return</span> <span class="o">(</span><span class="s2">"$ref"</span> <span class="o">==</span> <span class="s1">'none'</span><span class="o">)</span> <span class="o">?</span> <span class="s2">"$prBranch"</span> <span class="o">:</span> <span class="s2">"$ref"</span><span class="o">.</span><span class="na">split</span><span class="o">(</span><span class="s1">'refs/heads/'</span><span class="o">)[</span><span class="mi">1</span><span class="o">]</span>
<span class="o">}</span>
    
<span class="kt">def</span> <span class="nf">getSonarQubeParameters</span><span class="o">(</span><span class="n">branch</span><span class="o">)</span> <span class="o">{</span>
    <span class="kt">def</span> <span class="n">parameters</span> <span class="o">=</span> <span class="s2">""</span>
    <span class="k">if</span> <span class="o">(</span><span class="s2">"$ref"</span> <span class="o">==</span> <span class="s1">'none'</span><span class="o">)</span> <span class="o">{</span>
        <span class="n">parameters</span> <span class="o">+=</span> <span class="s2">"-Dsonar.pullrequest.key=${pullRequestNo} "</span>
        <span class="n">parameters</span> <span class="o">+=</span> <span class="s2">"-Dsonar.pullrequest.branch=${branch} "</span>
        <span class="n">parameters</span> <span class="o">+=</span> <span class="s2">"-Dsonar.pullrequest.base=${prTarget} "</span>
    <span class="o">}</span> <span class="k">else</span> <span class="o">{</span>
        <span class="n">parameters</span> <span class="o">+=</span> <span class="s2">"-Dsonar.branch.name=${branch} "</span>
    <span class="o">}</span>
    <span class="k">return</span> <span class="n">parameters</span>
<span class="o">}</span>
</code></pre></div>        </div>
      </li>
      <li><code class="language-plaintext highlighter-rouge">getSonarQubeParameters</code> 함수는 push 이벤트와 pull_request 이벤트일 때, soanrqube 에 전달할 파라미터가 다르기 때문에 분기처리 필요
        <ul>
          <li>$ref == none → pull request 이벤트</li>
          <li>$ref ≠ none → push 이벤트</li>
        </ul>
      </li>
      <li>Dsonar.token : sonarqube 에서 생성한 token</li>
      <li>Dsonar.projectKey : sonarqube 프로젝트 생성 시, 발급되는 키</li>
      <li>Dsonar.projectName : github repository 이름</li>
      <li>Dsonar.pullrequest.key : pull request 를 식별하기 위한 목적으로 pull request 번호</li>
      <li>Dsonar.pullrequest.branch : pull request 하는 브랜치명</li>
      <li>Dsonar.pullrequest.base : merge 당하는 브랜치명</li>
      <li>Dsonar.branch.name : push 하는 브랜치명</li>
    </ul>
  </li>
</ul>

<h3 id="sonarqube-setting--pr-decoration">sonarqube setting + pr decoration</h3>

<p>sonarqube 설정과 github pr decoration 은 이전 포스팅에서 수행해야할 Step 목록만 정의해줄테니, 이전 포스팅을 참고해라! 중복제거!</p>

<ul>
  <li><a href="https://lkhlkh23.github.io/sonarqube/2024/06/23/sonarqube-github.html"><strong>SonarQube 와 Github 연동 (with PR Decoration)</strong></a></li>
</ul>

<p><code class="language-plaintext highlighter-rouge">Step.0 ~ 6</code> 를 순차적으로 수행한다. 그리고 빌드 도구로 github action 이 아닌 jenkins 를 사용할 예정이기 때문에 <code class="language-plaintext highlighter-rouge">Step.7</code> 에서는 github action 에 대한 설정은 제외하고, build.gradle 에 대해서만 아래와 같이 정의한다.</p>

<div class="language-groovy highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">plugins</span> <span class="o">{</span>
	<span class="n">id</span> <span class="s1">'java'</span>
	<span class="n">id</span> <span class="s1">'org.springframework.boot'</span> <span class="n">version</span> <span class="s1">'3.3.0'</span>
	<span class="n">id</span> <span class="s1">'io.spring.dependency-management'</span> <span class="n">version</span> <span class="s1">'1.1.5'</span>
	<span class="n">id</span> <span class="s2">"org.sonarqube"</span> <span class="n">version</span> <span class="s2">"5.0.0.4638"</span>
<span class="o">}</span>

<span class="n">sonar</span> <span class="o">{</span>
	<span class="n">properties</span> <span class="o">{</span>
		<span class="n">property</span> <span class="s2">"sonar.projectKey"</span><span class="o">,</span> <span class="s2">"soanrqube project 추가하는 과정에서 발급받은 키"</span>
		<span class="n">property</span> <span class="s2">"sonar.projectName"</span><span class="o">,</span> <span class="s2">"프로젝트명"</span>
	<span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<p>그리고나서 pr decoration 을 위해서 <code class="language-plaintext highlighter-rouge">Step.8</code> 을 진행하면 끝! 위와 같이 진행하면 <code class="language-plaintext highlighter-rouge">soanrqube + github action + github</code> 구성이 완료된다! 끝!</p>

<h3 id="회고">회고</h3>

<p>그 누구도 시키지 않았지만, sonarqube 를 도입하게된 이유는 아래와 같다.</p>

<p>개발과 연관된 KPI 의 평가기준에 sonarqube 의 정적분석 결과가 지표가 되었다. 그러다보니 평소와 같이 개발 하고 평가 직전에 local 에 설치된 sonarqube 정적 분석 결과를 바탕으로 수정하는 경우가 발생했다. 이런 프로세스는 마치 개학 전날에 밀린 일기를 한꺼번에 작성하는 것과 같다고 생각한다. 결국은 코드의 품질에 전혀 도움이 되지 않는다.</p>

<p>그렇다고 pr 을 할때마다 개발자가 local 에 설치된 sonarqube 로 정적 분석을 실행하는 것도 매우 번거롭게 귀찮은 일이다. 그렇기 때문에 이러한 과정을 자동으로 지원해주는 기능이 필요하다고 생각했다. 그래서 도입을 하게되었다.</p>

<p><img src="https://raw.githubusercontent.com/lkhlkh23/lkhlkh23.github.io/master/images/2024-08-31/10.png" alt="10.png" /></p>

<p>하지만 … 진짜 이유는 업무 개선을 하고, 그에 따른 보상금을 GET 하기 위한 것이다! 기달려라! 20만원!</p>]]></content><author><name>lkhlkh23</name></author><category term="sonarqube" /><category term="sonarqube" /><category term="github" /><category term="pr" /><category term="decoration" /><category term="jenkins" /><summary type="html"><![CDATA[이전 포스팅에서 soanrqube + github action + github 구조를 이용해서 pull request 를 했을 때, 정적분석 결과를 pull request 에 decoration 을 하는 작업을 정리했다.]]></summary></entry><entry><title type="html">성능 테스트 도구 Vegeta</title><link href="https://lkhlkh23.github.io/jekyll-theme-yat/test/2024/08/30/vegeta.html" rel="alternate" type="text/html" title="성능 테스트 도구 Vegeta" /><published>2024-08-30T00:00:00+00:00</published><updated>2024-08-30T00:00:00+00:00</updated><id>https://lkhlkh23.github.io/jekyll-theme-yat/test/2024/08/30/vegeta</id><content type="html" xml:base="https://lkhlkh23.github.io/jekyll-theme-yat/test/2024/08/30/vegeta.html"><![CDATA[<p>최근에 개발팀 내부에서 간단하게 API 서비스에 대한 성능 테스트를 진행했다. 성능 테스트를 위해 다양한 도구들이 있었지만, 간단하게 이용할 수 있는 <code class="language-plaintext highlighter-rouge">vegeta</code> 를 이용했다.</p>

<p>대부분 <code class="language-plaintext highlighter-rouge">jmeter</code>, <code class="language-plaintext highlighter-rouge">locust</code> 는 친숙할 수 있지만, <code class="language-plaintext highlighter-rouge">vegeta</code> 는 조금 생소할 수 있다. 아마도 가장 먼저 드래곤볼을 생각했을 것이다! 맞다! 드래곤볼의 대표 전투력 측정기인 vegeta 를 모델로 만든 오픈소스이다.</p>

<h3 id="what-is-vegeta">What is Vegeta..?</h3>

<p><code class="language-plaintext highlighter-rouge">vegeta 는 HTTP 서비스의 성능 테스트를 위한 오픈소스 부하 생성기</code>이다. 성능 테스트는 응답 속도와 안정성을 검증하기 위해 필수적으로 수행되는 작업이다. vegeta 가 제공하는 다양한 기능을 통해 부하를 생성하고, 응답 시간, 시스템 처리량, 지연시간, 오류율 등의 다양한 메트릭을 수집할 수 있다.</p>

<h3 id="what-is-vegeta-features">What is Vegeta features..?</h3>

<p><a href="https://github.com/tsenart/vegeta">vegeta github</a> 에서는 vegeta 에 대한 특징을 아래와 같이 정의하고 있다. 이해하기 쉽게 하나씩 살펴보자!</p>

<ul>
  <li>Usable as a command line tool and a Go library.</li>
  <li>CLI designed with UNIX composability in mind.</li>
  <li>Avoids nasty Coordinated Omission.</li>
  <li>Extensive reporting functionality.</li>
  <li>Easy to install and run (static binary, package managers, etc).</li>
</ul>

<p><strong>Usable as a command line tool and a Go library.</strong></p>

<p><strong>→ CLI 와 Go 라이브러리로 사용 가능</strong></p>

<p>vegeta 는 두가지 사용법을 제공한다.</p>

<ol>
  <li>사용자가 터미널에 직접 명령어 입력해서 수행하는 CLI 방식</li>
  <li>Go 언어로 스크립트 작성해서 이용하는 방식</li>
</ol>

<p><strong>CLI designed with UNIX composability in mind.</strong></p>

<p><strong>→ UNIX 철학을 반영한 CLI 디자인</strong></p>

<p>먼저 UNIX 철학에 대해 간단히 알아보자!</p>

<ol>
  <li>하나의 프로그램은 하나의 역할만 하도록 설계한다.</li>
  <li>하나의 역할만 수행하는 작은 프로그램을 조합해서 복잡한 작업을 수행한다.
아래와 같이 파이프 (<code class="language-plaintext highlighter-rouge">|</code>) 를 이용해서 프로그램을 조합해서 사용할 수 있다.</li>
</ol>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">ls</span> | <span class="nb">grep</span> <span class="s2">"txt"</span>
</code></pre></div></div>

<ol>
  <li>소프트웨어 툴은 서로 협력할 수 있어야 한다.
도구들이 서로의 출력과 입력을 쉽게 연결할 수 있도록 텍스트 기반의 인터페이스를 제공하는 것을 포함한다.</li>
</ol>

<p>vegeta 는 UNIX 철학에 따라 파이프를 통해 다른 도구와 함께 사용할 수 있고, 입출력을 파일이나 다른 명령어와 쉽게 연결할 수 있다.</p>

<p><strong>Avoids nasty Coordinated Omission.</strong></p>

<p><strong>→ Coordinated Omission 문제 회피</strong></p>

<p><code class="language-plaintext highlighter-rouge">Coordinated Omission</code> 는 부하 발생 시 시스템의 응답 시간이 느려질 때, 테스트 도구가 의도치 않게 요청을 줄이면서 실제 시스템의 성능 저하를 제대로 측정하지 못하는 경우를 의미한다. vegeta 는 이러한 문제를 피하기 위한 메커니즘을 갖추고 있기 때문에 성능을 더 정확하게 평가할 수 있다.</p>

<p><strong>Extensive reporting functionality.</strong></p>

<p><strong>→ 광범위한 보고 기능</strong></p>

<p>평균 응답 시간, 성공률, 처리량, 지연 분포 등의 다양한 성능 지표를 분석하고 시각화할 수 있는 레포트 기능을 제공한다. 사용자는 텍스트, 그래프 등 다양한 형식으로 보고서를 생성할 수 있다.</p>

<p><strong>Easy to install and run (static binary, package managers, etc).</strong></p>

<p><strong>→ 설치와 실행이 쉬움</strong></p>

<p>베지터는 설치와 실행이 매우 간편하다. 정적 바이너리 파일로 제공되기 때문에, 별도의 라이브러리나 종속성을 설치할 필요 없이 다운로드 후 바로 실행할 수 있다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>brew update <span class="o">&amp;&amp;</span> brew <span class="nb">install </span>vegeta
</code></pre></div></div>

<h3 id="what-is-vegeta-limitation">What is Vegeta limitation..?</h3>

<p><strong>제한적인 프로토콜</strong></p>

<p>앞에서 <code class="language-plaintext highlighter-rouge">vegeta 는 HTTP 서비스의 성능 테스트를 위한 오픈소스 부하 생성기</code> 라고 정의했다.  vegeta 는 HTTP, HTTPS 에 최적화되있기 때문에 이를 제외한 다른 프로토콜에 대해서는 성능 테스트를 진행하기에는 제한이 있다.</p>

<p><strong>복잡한 시나리오 시뮬레이션의 어려움</strong></p>

<p>vegeta 는 단순한 HTTP 요청을 테스트 하기 적합하며, 특정한 순서나 상태를 필요로 하는 복잡한 시나리오에서는 적합하지 않다. 개인적으로 HTTP 기반의 API 를 테스트할 때 적합하다고 생각한다.</p>

<p><strong>리포트 시각화 및 분석 기능의 한계</strong></p>

<p>vegeta 에서도 다양한 지표와 시각화할 수 있는 기능은 제공하지만, <code class="language-plaintext highlighter-rouge">jmeter</code> 와 <code class="language-plaintext highlighter-rouge">locust</code> 와 같은 수준의 기능은 제공하지는 않다.</p>

<h3 id="why-did-i-use-vegeta">Why did I use Vegeta?</h3>

<p>vegeta 의 장점과 한계점에 대해 간단히 알아봤다. 그럼에도 불구하고 나는 왜?! 이번 성능테스트에서 vegeta 를 사용했을까?!</p>

<ul>
  <li>예정되있지 않는 갑작스러운 성능 테스트로 인해서 간단하고 빠르게 설치가 필요</li>
  <li>복잡한 시나리오가 아닌, HTTP API 의 간단한 호출</li>
  <li>평균 응답 시간, 성공률, 처리량, 지연 분포를 포함한 간단한 메트릭 필요</li>
</ul>

<p>위와 같은 상황과 요구사항이 있기 때문에 vegeta 가 가장 적합하다고 생각했다. 그래서 사용했다! 그러하다!</p>

<h3 id="how-to-use-vegeta">How to use Vegeta..?</h3>

<p><strong>HTTP GET API 1개 테스트</strong></p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">echo</span> <span class="s2">"GET http://localhost:8080/coupons"</span> | <span class="se">\</span>
vegeta attack <span class="se">\</span>
  <span class="nt">-header</span> <span class="s2">"Cache-Control: no-cache"</span> <span class="se">\</span>
  <span class="nt">-duration</span><span class="o">=</span>5s <span class="se">\</span>
  <span class="nt">-rate</span><span class="o">=</span>1 <span class="se">\</span>
| <span class="nb">tee </span>results.txt | vegeta report 
</code></pre></div></div>

<ul>
  <li><code class="language-plaintext highlighter-rouge">vegeta attack</code> : 부하를 발생 명령어</li>
  <li><code class="language-plaintext highlighter-rouge">-header</code> : HTTP 요청에 header 값 정의</li>
  <li><code class="language-plaintext highlighter-rouge">-duration</code> : 테스트 지속 시간 (0 : forever)</li>
  <li><code class="language-plaintext highlighter-rouge">-rate</code> : 초당 요청 수</li>
  <li><code class="language-plaintext highlighter-rouge">tee results.txt</code> : 부하 결과를 results.txt 에 저장</li>
  <li><code class="language-plaintext highlighter-rouge">vegeta report</code> : 부하 결과를 콘솔에 노출</li>
</ul>

<p>vegeta attack 에 더 많은 옵션을 부여할 수 있는데, 그것은 <a href="https://github.com/tsenart/vegeta">vegeta github</a> 를 참고하면 된다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Requests      <span class="o">[</span>total, rate, throughput]         5, 1.25, 1.25
Duration      <span class="o">[</span>total, attack, <span class="nb">wait</span><span class="o">]</span>             4.004s, 3.998s, 5.688ms
Latencies     <span class="o">[</span>min, mean, 50, 90, 95, 99, max]  5.337ms, 8.131ms, 5.688ms, 18.432ms, 18.432ms, 18.432ms, 18.432ms
Bytes In      <span class="o">[</span>total, mean]                     95, 19.00
Bytes Out     <span class="o">[</span>total, mean]                     0, 0.00
Success       <span class="o">[</span>ratio]                           100.00%
Status Codes  <span class="o">[</span>code:count]                      200:5
</code></pre></div></div>

<ul>
  <li>Requests
    <ul>
      <li><code class="language-plaintext highlighter-rouge">total</code> : 요청 수</li>
      <li><code class="language-plaintext highlighter-rouge">rate</code> : 초당 요청 수</li>
      <li><code class="language-plaintext highlighter-rouge">throughput</code> : 초당 처리한 수</li>
    </ul>
  </li>
  <li>Duration
    <ul>
      <li><code class="language-plaintext highlighter-rouge">total</code> : 총 소요시간</li>
      <li><code class="language-plaintext highlighter-rouge">attack</code> : 총 공격 시간</li>
      <li><code class="language-plaintext highlighter-rouge">wait</code> : 요청이 서버에 도달한 후 응답을 받기까지 걸린 평균 시간</li>
    </ul>
  </li>
  <li>Latencies
    <ul>
      <li><code class="language-plaintext highlighter-rouge">min</code> : 요청에 대한 최소 응답시간</li>
      <li><code class="language-plaintext highlighter-rouge">mean</code> : 요청에 대한 평균 응답시간</li>
      <li><code class="language-plaintext highlighter-rouge">max</code> : 요청에 대한 최대 응답시간</li>
      <li><code class="language-plaintext highlighter-rouge">50, 90, 95, 99</code> : 예) 50번째 백분위수 지연 시간</li>
    </ul>
  </li>
  <li>Success
    <ul>
      <li><code class="language-plaintext highlighter-rouge">ratio</code> : 성공률</li>
    </ul>
  </li>
  <li>Status Codes
    <ul>
      <li><code class="language-plaintext highlighter-rouge">code:count</code> : Http상태:수</li>
    </ul>
  </li>
</ul>

<p><strong>HTTP GET API N개 테스트</strong></p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">cat </span>apis.txt | vegeta attack <span class="se">\</span>
  <span class="nt">-duration</span><span class="o">=</span>10s <span class="se">\</span>
  <span class="nt">-rate</span><span class="o">=</span>1 <span class="se">\</span>
| <span class="nb">tee </span>results.txt <span class="se">\</span>
| vegeta report
</code></pre></div></div>

<p><strong>apis.txt</strong></p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>GET http://localhost:8080/coupons/c-1
GET http://localhost:8080/coupons
GET http://localhostL8080/coupons/c-2
</code></pre></div></div>

<p>위와 같이 apis.txt 를 설정하면, 출력 결과가 아래와 같다.</p>

<p>순차적으로 호출하고, 총 10번을 호출한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>getCoupon : c-1
getCoupons : <span class="o">[</span>c-1, c-2, c-3]
getCoupon : c-2
getCoupon : c-1
getCoupons : <span class="o">[</span>c-1, c-2, c-3]
getCoupon : c-2
getCoupon : c-1
getCoupons : <span class="o">[</span>c-1, c-2, c-3]
getCoupon : c-2
getCoupon : c-1
</code></pre></div></div>

<p><strong>레포트 기능</strong></p>

<p><strong>정적결과</strong></p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">cat </span>apis.txt | vegeta attack <span class="se">\</span>
  <span class="nt">-duration</span><span class="o">=</span>10s <span class="se">\</span>
  <span class="nt">-rate</span><span class="o">=</span>1 <span class="se">\</span>
| <span class="nb">tee </span>results.txt <span class="se">\</span>
| vegeta report

vegeta plot results.txt <span class="o">&gt;</span> results.html
</code></pre></div></div>

<ul>
  <li><code class="language-plaintext highlighter-rouge">vegeta plot</code> : 차트로 시각화</li>
</ul>

<p><img src="https://raw.githubusercontent.com/lkhlkh23/lkhlkh23.github.io/master/images/2024-08-30/0.png" alt="0.png" /></p>

<p><strong>동적결과</strong></p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>brew <span class="nb">install </span>rs/tap/jplot
brew <span class="nb">install </span>rs/tap/jaggr

<span class="nb">echo</span> <span class="s1">'GET http://localhost:8080/coupons'</span> | <span class="se">\</span>
    vegeta attack <span class="nt">-rate</span> 5 <span class="nt">-duration</span> 10m | vegeta encode | <span class="se">\</span>
    jaggr @count<span class="o">=</span>rps <span class="se">\</span>
          hist<span class="se">\[</span>100,200,300,400,500<span class="se">\]</span>:code <span class="se">\</span>
          p25,p50,p95:latency <span class="se">\</span>
          <span class="nb">sum</span>:bytes_in <span class="se">\</span>
          <span class="nb">sum</span>:bytes_out | <span class="se">\</span>
    jplot rps+code.hist.100+code.hist.200+code.hist.300+code.hist.400+code.hist.500 <span class="se">\</span>
          latency.p95+latency.p50+latency.p25 <span class="se">\</span>
          bytes_in.sum+bytes_out.sum
</code></pre></div></div>

<p><img src="https://raw.githubusercontent.com/lkhlkh23/lkhlkh23.github.io/master/images/2024-08-30/1.png" alt="0.png" /></p>

<p>위와 같은 그래프가 실시간으로 계속 그려진다.</p>

<p>지금까지 vegeta 에 대해서 간단하게 알아봤다. 직관적인 도구고 아니고 옵션도 많기 때문에 비개발자가 사용하는 것은 조금 어려울 수 있다고 생각한다. 하지만, 간단한 시나리오에서 성능 테스트를 진행하기에는 충분할 수 있다고 생각한다.</p>]]></content><author><name>lkhlkh23</name></author><category term="test" /><category term="test" /><category term="performance" /><summary type="html"><![CDATA[최근에 개발팀 내부에서 간단하게 API 서비스에 대한 성능 테스트를 진행했다. 성능 테스트를 위해 다양한 도구들이 있었지만, 간단하게 이용할 수 있는 vegeta 를 이용했다.]]></summary></entry><entry><title type="html">Eureka Server 재시작 시, 서비스 등록 실패</title><link href="https://lkhlkh23.github.io/jekyll-theme-yat/spring/2024/07/30/eureka-server-restart-issue.html" rel="alternate" type="text/html" title="Eureka Server 재시작 시, 서비스 등록 실패" /><published>2024-07-30T00:00:00+00:00</published><updated>2024-07-30T00:00:00+00:00</updated><id>https://lkhlkh23.github.io/jekyll-theme-yat/spring/2024/07/30/eureka-server-restart-issue</id><content type="html" xml:base="https://lkhlkh23.github.io/jekyll-theme-yat/spring/2024/07/30/eureka-server-restart-issue.html"><![CDATA[<p>Eureka Server 재시작 시, Eureka Client 가 자동적으로 서비스 등록되지 않는 이슈가 발생했다. 모든 Eureka Client 가 서비스 등록에 실패하는 것이 아닌 특정 Eureka Client 대상으로만 발생했다.</p>

<p>오늘은 해당 이슈에 대한 원인과 해결방법에 대해 정리하려고 한다.</p>

<h3 id="symptoms">Symptoms</h3>

<p>이슈가 있는 서비스는 Eureka Client-2 이다. Eureka Server 재시작 시, Eureka Client-2 만 자동으로 서비스 등록이 되지 않는다.</p>

<ul>
  <li>Eureka Server
    <ul>
      <li>SpringBoot : 3.2.1</li>
      <li>SpringCloud : 2023.0.0</li>
      <li>Java17</li>
    </ul>
  </li>
  <li>Eureka Client-1 (coupon)
    <ul>
      <li>SpringBoot : 3.2.1</li>
      <li>SpringCloud : 2023.0.0</li>
      <li>Java17</li>
    </ul>
  </li>
  <li>eureka client-2 (order) (이슈)
    <ul>
      <li>SpringBoot : 2.7.12</li>
      <li>SpringCloud : 2021.0.3</li>
      <li>Java17</li>
    </ul>
  </li>
</ul>

<p>그리고, 아래와 같은 오류가 발생한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>2024-07-30 15:53:31.024  WARN 59292 <span class="nt">---</span> <span class="o">[</span>tbeatExecutor-0] c.n.d.s.t.d.RetryableEurekaHttpClient    : Request execution failed with message: Error <span class="k">while </span>extracting response <span class="k">for </span><span class="nb">type</span> <span class="o">[</span>class com.netflix.appinfo.InstanceInfo] and content <span class="nb">type</span> <span class="o">[</span>application/json]<span class="p">;</span> nested exception is org.springframework.http.converter.HttpMessageNotReadableException: JSON parse error: Root name <span class="o">(</span><span class="s1">'timestamp'</span><span class="o">)</span> does not match expected <span class="o">(</span><span class="s1">'instance'</span><span class="o">)</span> <span class="k">for </span><span class="nb">type</span> <span class="sb">`</span>com.netflix.appinfo.InstanceInfo<span class="sb">`</span><span class="p">;</span> nested exception is com.fasterxml.jackson.databind.exc.MismatchedInputException: Root name <span class="o">(</span><span class="s1">'timestamp'</span><span class="o">)</span> does not match expected <span class="o">(</span><span class="s1">'instance'</span><span class="o">)</span> <span class="k">for </span><span class="nb">type</span> <span class="sb">`</span>com.netflix.appinfo.InstanceInfo<span class="sb">`</span>
 at <span class="o">[</span>Source: <span class="o">(</span>org.springframework.util.StreamUtils<span class="nv">$NonClosingInputStream</span><span class="o">)</span><span class="p">;</span> line: 1, column: 2] <span class="o">(</span>through reference chain: com.netflix.appinfo.InstanceInfo[<span class="s2">"timestamp"</span><span class="o">])</span>
2024-07-30 15:53:31.026 ERROR 59292 <span class="nt">---</span> <span class="o">[</span>tbeatExecutor-0] com.netflix.discovery.DiscoveryClient    : DiscoveryClient_ORDER/192.168.45.109:order:8082 - was unable to send heartbeat!
</code></pre></div></div>

<h3 id="solutions">Solutions</h3>

<p><strong>원인은 Eureka Server 와 Eureka Client 의 버전 호환성으로 발생하는 이슈</strong>이다. 해결책은 심플하다! <strong>버전 호환성을 통일</strong>하면 된다. 이렇게 간단한 이유라면 포스팅을 정리하지도 않았다.</p>

<p>현재 Eureka Client-2 는 QA 가 모두 완료되었기 때문에 버전을 변경하는 것은 현실적으로 어렵다. 그래서 Eureka Client 의 버전을 다운그레이드 하는 것은 어렵다. 제안하는 해결책은 2개이다.</p>

<p><strong>Solution-1. Eureka Server 버전 다운그레이드</strong></p>

<p>Eureka Server 를 아래와 같이 버전 다운그레이드 했다. 다운그레이드 이후에 Eureka Server 재시작을 해보니 정상적으로 모든 Eureka Client 가 등록되었다.</p>

<ul>
  <li>SpringBoot <del>3.2.1</del> → 2.7.12</li>
  <li>SpringCloud : <del>2023.0.0</del> → 2021.0.3</li>
  <li>Java17</li>
</ul>

<p><strong>Solution-2. ErrorController 구현</strong></p>

<p>Eureka Server 에 아래와 같이 Controller 를 추가한다면, 버전 변경없이 쉽게 해결할 수 있다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@RestController</span>
<span class="kd">public</span> <span class="kd">class</span> <span class="nc">EurekaErrorController</span> <span class="kd">implements</span> <span class="nc">ErrorController</span> <span class="o">{</span>

	<span class="nd">@RequestMapping</span><span class="o">(</span><span class="s">"/error"</span><span class="o">)</span>
	<span class="kd">public</span> <span class="nc">ResponseEntity</span><span class="o">&lt;</span><span class="nc">Void</span><span class="o">&gt;</span> <span class="nf">error</span><span class="o">()</span> <span class="o">{</span>
		<span class="k">return</span> <span class="nc">ResponseEntity</span><span class="o">.</span><span class="na">notFound</span><span class="o">().</span><span class="na">build</span><span class="o">();</span>
	<span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<p>팀내 시니어 개발자분이 가이드 주신 방법이다. 감사합니다 :D</p>

<p><img src="https://raw.githubusercontent.com/lkhlkh23/lkhlkh23.github.io/master/images/2024-07-30/banner.png" alt="banner.png" /></p>

<p>내가 생각한 <code class="language-plaintext highlighter-rouge">Solution-1</code> 은 다운그레이드로 인한 사이드 이펙트가 발생할 수 있기 때문에 좋은 방법은 아니다. 그리고 업그레이드도 아닌 다운그레이드라니 …</p>

<h3 id="cause">Cause</h3>

<p>원인은 앞서 설명했듯이 <strong>버전 호환성으로 인한 문제</strong>이다. SpringBoot 2.x 와 SpringBoot 3.x 는 에러 응답 객체가 다르다.</p>

<ul>
  <li>SpringBoot 2.x : Body 가 없는 응답</li>
  <li>SpringBoot 3.x : Body 가 있는 응답</li>
</ul>

<p>그렇기 때문에 위 오류는 <strong>에러 응답을 파싱하는 과정에서 발생</strong>한 것이다. 내가 이 문제에서 가장 이해가 되지 않았던 부분은 Eureka Client 가 시작 시 Eureka Server 에 정상적으로 서비스 등록이 되는데, Eureka Server 가 재시작될 때는 왜?! 서비스 등록이 되지 않는지가 제일 이해가 되지 않았다.</p>

<p>그래서 Eureka Client 의 <code class="language-plaintext highlighter-rouge">DiscoveryClient</code> 클래스를 디비깅해봤다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">private</span> <span class="kd">class</span> <span class="nc">HeartbeatThread</span> <span class="kd">implements</span> <span class="nc">Runnable</span> <span class="o">{</span>

        <span class="kd">public</span> <span class="kt">void</span> <span class="nf">run</span><span class="o">()</span> <span class="o">{</span>
            <span class="k">if</span> <span class="o">(</span><span class="n">renew</span><span class="o">())</span> <span class="o">{</span>
                <span class="n">lastSuccessfulHeartbeatTimestamp</span> <span class="o">=</span> <span class="nc">System</span><span class="o">.</span><span class="na">currentTimeMillis</span><span class="o">();</span>
            <span class="o">}</span>
        <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kt">boolean</span> <span class="nf">renew</span><span class="o">()</span> <span class="o">{</span>
        <span class="nc">EurekaHttpResponse</span><span class="o">&lt;</span><span class="nc">InstanceInfo</span><span class="o">&gt;</span> <span class="n">httpResponse</span><span class="o">;</span>
        <span class="k">try</span> <span class="o">{</span>
            <span class="n">httpResponse</span> <span class="o">=</span> <span class="n">eurekaTransport</span><span class="o">.</span><span class="na">registrationClient</span><span class="o">.</span><span class="na">sendHeartBeat</span><span class="o">(</span><span class="n">instanceInfo</span><span class="o">.</span><span class="na">getAppName</span><span class="o">(),</span> <span class="n">instanceInfo</span><span class="o">.</span><span class="na">getId</span><span class="o">(),</span> <span class="n">instanceInfo</span><span class="o">,</span> <span class="kc">null</span><span class="o">);</span>
            <span class="n">logger</span><span class="o">.</span><span class="na">debug</span><span class="o">(</span><span class="no">PREFIX</span> <span class="o">+</span> <span class="s">"{} - Heartbeat status: {}"</span><span class="o">,</span> <span class="n">appPathIdentifier</span><span class="o">,</span> <span class="n">httpResponse</span><span class="o">.</span><span class="na">getStatusCode</span><span class="o">());</span>
            <span class="k">if</span> <span class="o">(</span><span class="n">httpResponse</span><span class="o">.</span><span class="na">getStatusCode</span><span class="o">()</span> <span class="o">==</span> <span class="nc">Status</span><span class="o">.</span><span class="na">NOT_FOUND</span><span class="o">.</span><span class="na">getStatusCode</span><span class="o">())</span> <span class="o">{</span>
                <span class="no">REREGISTER_COUNTER</span><span class="o">.</span><span class="na">increment</span><span class="o">();</span>
                <span class="n">logger</span><span class="o">.</span><span class="na">info</span><span class="o">(</span><span class="no">PREFIX</span> <span class="o">+</span> <span class="s">"{} - Re-registering apps/{}"</span><span class="o">,</span> <span class="n">appPathIdentifier</span><span class="o">,</span> <span class="n">instanceInfo</span><span class="o">.</span><span class="na">getAppName</span><span class="o">());</span>
                <span class="kt">long</span> <span class="n">timestamp</span> <span class="o">=</span> <span class="n">instanceInfo</span><span class="o">.</span><span class="na">setIsDirtyWithTime</span><span class="o">();</span>
                <span class="kt">boolean</span> <span class="n">success</span> <span class="o">=</span> <span class="n">register</span><span class="o">();</span>
                <span class="k">if</span> <span class="o">(</span><span class="n">success</span><span class="o">)</span> <span class="o">{</span>
                    <span class="n">instanceInfo</span><span class="o">.</span><span class="na">unsetIsDirty</span><span class="o">(</span><span class="n">timestamp</span><span class="o">);</span>
                <span class="o">}</span>
                <span class="k">return</span> <span class="n">success</span><span class="o">;</span>
            <span class="o">}</span>
            <span class="k">return</span> <span class="n">httpResponse</span><span class="o">.</span><span class="na">getStatusCode</span><span class="o">()</span> <span class="o">==</span> <span class="nc">Status</span><span class="o">.</span><span class="na">OK</span><span class="o">.</span><span class="na">getStatusCode</span><span class="o">();</span>
        <span class="o">}</span> <span class="k">catch</span> <span class="o">(</span><span class="nc">Throwable</span> <span class="n">e</span><span class="o">)</span> <span class="o">{</span>
            <span class="n">logger</span><span class="o">.</span><span class="na">error</span><span class="o">(</span><span class="no">PREFIX</span> <span class="o">+</span> <span class="s">"{} - was unable to send heartbeat!"</span><span class="o">,</span> <span class="n">appPathIdentifier</span><span class="o">,</span> <span class="n">e</span><span class="o">);</span>
            <span class="k">return</span> <span class="kc">false</span><span class="o">;</span>
        <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<p>Eureka Client 는 주기적으로 HeartBeat 를 Eureka Server 에 전달한다. HeartBeat 전달하는 과정에서 404 오류가 발생을 하면, <code class="language-plaintext highlighter-rouge">register()</code> 메소드를 이용해서 Eureka Server 에 서비스 등록을 한다. 그런데 해결책을 적용하기 전에는 Eureka Server 로 부터 전달받은 응답을 파싱하는 과정에서 예외가 발생했기 때문에 <code class="language-plaintext highlighter-rouge">register()</code> 메소드를 호출하지 못해서 서비스 등록을 하지 못했다.</p>

<p>그래서 <code class="language-plaintext highlighter-rouge">Solution-2</code> 는 Eureka Server 에서 404 오류에 대해서는 Body 가 없는 응답값으로 커스텀해서 내려주는 방식으로 해결하고 있다. Body 가 없기 때문에 Eureka Client-2 에서는 응답을 정상적으로 파싱할 수 있게 되었고, 결국은 <code class="language-plaintext highlighter-rouge">register()</code> 메소드를 호출해서 서비스 등록을 할 수 있게 되었다.</p>

<p><code class="language-plaintext highlighter-rouge">Solution-1</code> 은 Eureka Server 가 2.x 버전으로 다운그레이드 했기 때문에 Body 가 없는 형태로 응답을 하게 되었다. 그래서 Eureka Client-2 가 응답을 정상적으로 파싱할 수 있게 된 것이다.</p>

<p>결국, Eureka Client 가 시작 시 Eureka Server 에 정상적으로 서비스 등록이 되는데, Eureka Server 가 재시작될 때 서비스 등록이 되지 않았던 이유는 서비스 등록이 문제가 아니라, HeartBeat 처리하는 과정에서 오류가 발생했기 때문이었다.</p>

<h3 id="conclusion">Conclusion</h3>

<p>오류 덕분에 Eureka 에 대해 더 공부할 수 있게 되었다. 하지만 오류는 싫다. 긍정적인척을 했다. 순간 나의 이중성에 조금 놀랬다.</p>

<p><img src="https://raw.githubusercontent.com/lkhlkh23/lkhlkh23.github.io/master/images/2024-07-30/0.png" alt="0.png" /></p>]]></content><author><name>lkhlkh23</name></author><category term="spring" /><category term="spring" /><category term="eureka" /><summary type="html"><![CDATA[Eureka Server 재시작 시, Eureka Client 가 자동적으로 서비스 등록되지 않는 이슈가 발생했다. 모든 Eureka Client 가 서비스 등록에 실패하는 것이 아닌 특정 Eureka Client 대상으로만 발생했다.]]></summary></entry><entry><title type="html">불변객체를 사용해야 하는 이유 (이펙티브 자바 아이템 17)</title><link href="https://lkhlkh23.github.io/jekyll-theme-yat/java/2024/07/22/why-use-immutable.html" rel="alternate" type="text/html" title="불변객체를 사용해야 하는 이유 (이펙티브 자바 아이템 17)" /><published>2024-07-22T00:00:00+00:00</published><updated>2024-07-22T00:00:00+00:00</updated><id>https://lkhlkh23.github.io/jekyll-theme-yat/java/2024/07/22/why-use-immutable</id><content type="html" xml:base="https://lkhlkh23.github.io/jekyll-theme-yat/java/2024/07/22/why-use-immutable.html"><![CDATA[<p>final 을 써야하는 이유에 대해 포스팅을 하다가, 불변객체를 써야하는 이유까지 다시 복습하게 되었다. 다시 이펙티브 자바를 읽었다. 이 책은 정말 매력적이다. 읽을때마다 항상 새롭다. 늘 뇌가 리셋되었다고 할까?! 나의 뇌를 순수하게 만든다고 할까?!</p>

<h3 id="what-is-immutable-class">What is Immutable Class</h3>

<p>객체 내부에 정의된 필드의 값을 수정할 수 없는 클래스이다. 불변 객체에 정의된 필드는 GC 에 의해 소멸되는 순간까지 절대 변하지 않는다.</p>

<h3 id="rules-of-immutable-class">Rules of Immutable Class</h3>

<p>Rule.1 setter 와 같은 객체의 상태를 변경하는 메소드 제공 금지</p>

<p>Rule.2 클래스를 상속할 수 없도록 final class 로 선언</p>

<p>Rule.3 클래스 내부의 모든 필드 private final 로 선언</p>

<p>Rule.4 클래스 내부 필드에 가변 객체가 있다면, 가변 객체에 접근하지 못하도록 설정 필요</p>

<p>처음에 Rule.3 과 Rule.4 의 규칙이 충돌되는 것이 아닌가?! 라는 의문을 가졌다. 모든 필드는 private final 로 선언해서 수정할 수 없는 형태로 만드는데, 가변객체가 존재할 수 있다고?! 이상했다. 나와 같이 혼란이 왔다면, 아래 코드를 보자</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">public</span> <span class="kd">final</span> <span class="kd">class</span> <span class="nc">Period</span> <span class="o">{</span>

	<span class="kd">private</span> <span class="kd">final</span> <span class="nc">Date</span> <span class="n">startAt</span><span class="o">;</span>
	<span class="kd">private</span> <span class="kd">final</span> <span class="nc">Date</span> <span class="n">endAt</span><span class="o">;</span>

	<span class="kd">public</span> <span class="nf">Period</span><span class="o">(</span><span class="nc">Date</span> <span class="n">startAt</span><span class="o">,</span> <span class="nc">Date</span> <span class="n">endAt</span><span class="o">)</span> <span class="o">{</span>
		<span class="k">this</span><span class="o">.</span><span class="na">startAt</span> <span class="o">=</span> <span class="n">startAt</span><span class="o">;</span>
		<span class="k">this</span><span class="o">.</span><span class="na">endAt</span> <span class="o">=</span> <span class="n">endAt</span><span class="o">;</span>
	<span class="o">}</span>

	<span class="kd">public</span> <span class="nc">Date</span> <span class="nf">getStartAt</span><span class="o">()</span> <span class="o">{</span>
		<span class="k">return</span> <span class="n">startAt</span><span class="o">;</span>
	<span class="o">}</span>

	<span class="kd">public</span> <span class="nc">Date</span> <span class="nf">getEndAt</span><span class="o">()</span> <span class="o">{</span>
		<span class="k">return</span> <span class="n">endAt</span><span class="o">;</span>
	<span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<p>여기서 퀴즈! Period 클래스는 불변 클래스의 규칙을 모두 준수하고 있는가?!</p>

<p><img src="https://raw.githubusercontent.com/lkhlkh23/lkhlkh23.github.io/master/images/2024-07-22/0.png" alt="0.png" /></p>

<p>정답은 No! Rule.4 를 준수하고 있지 않다. 만약 Date 가 아닌 LocalDateTime 이었다면 불변 클래스의 규칙을 모두 준수했을 것이다. 이유는 Date 는 setYear() 과 같은 메소드를 통해 내부 필드의 값을 변경할 수 있지만, <strong>LocalDateTime 은 내부 필드의 값을 변경할 수 없는 불변클래스</strong> 이다. Date 가 불변클래스가 아니기 때문에 테스트 코드와 같이 값을 변경해서 문제를 발생시킬 수 있다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">public</span> <span class="kd">class</span> <span class="nc">ImmutableTest</span> <span class="o">{</span>

  <span class="nd">@Test</span>
  <span class="kt">void</span> <span class="nf">test_copy_protect_1</span><span class="o">()</span> <span class="o">{</span>
    <span class="kd">final</span> <span class="nc">Date</span> <span class="n">startAt</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">Date</span><span class="o">();</span>
    <span class="kd">final</span> <span class="nc">Date</span> <span class="n">endAt</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">Date</span><span class="o">();</span>
    <span class="kd">final</span> <span class="nc">Period</span> <span class="n">period</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">Period</span><span class="o">(</span><span class="n">startAt</span><span class="o">,</span> <span class="n">endAt</span><span class="o">);</span>

    <span class="nc">System</span><span class="o">.</span><span class="na">out</span><span class="o">.</span><span class="na">println</span><span class="o">(</span><span class="s">"startAt : "</span> <span class="o">+</span> <span class="n">period</span><span class="o">.</span><span class="na">getStartAt</span><span class="o">());</span>

    <span class="n">period</span><span class="o">.</span><span class="na">getStartAt</span><span class="o">().</span><span class="na">setMonth</span><span class="o">(</span><span class="mi">0</span><span class="o">);</span>
    <span class="nc">System</span><span class="o">.</span><span class="na">out</span><span class="o">.</span><span class="na">println</span><span class="o">(</span><span class="s">"startAt : "</span> <span class="o">+</span> <span class="n">period</span><span class="o">.</span><span class="na">getStartAt</span><span class="o">());</span>
  <span class="o">}</span>

  <span class="nd">@Test</span>
  <span class="kt">void</span> <span class="nf">test_copy_protect_2</span><span class="o">()</span> <span class="o">{</span>
    <span class="kd">final</span> <span class="nc">Date</span> <span class="n">startAt</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">Date</span><span class="o">();</span>
    <span class="kd">final</span> <span class="nc">Date</span> <span class="n">endAt</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">Date</span><span class="o">();</span>
    <span class="kd">final</span> <span class="nc">Period</span> <span class="n">period</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">Period</span><span class="o">(</span><span class="n">startAt</span><span class="o">,</span> <span class="n">endAt</span><span class="o">);</span>
    <span class="n">startAt</span><span class="o">.</span><span class="na">setMonth</span><span class="o">(</span><span class="mi">3</span><span class="o">);</span>

    <span class="nc">System</span><span class="o">.</span><span class="na">out</span><span class="o">.</span><span class="na">println</span><span class="o">(</span><span class="s">"startAt : "</span> <span class="o">+</span> <span class="n">period</span><span class="o">.</span><span class="na">getStartAt</span><span class="o">());</span>
  <span class="o">}</span>

<span class="o">}</span>
</code></pre></div></div>

<p>위와같은 문제를 해결하기 위해서는 방어적 복사본을 적용해야 한다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">public</span> <span class="kd">final</span> <span class="kd">class</span> <span class="nc">Period</span> <span class="o">{</span>

	<span class="kd">private</span> <span class="kd">final</span> <span class="nc">Date</span> <span class="n">startAt</span><span class="o">;</span>
	<span class="kd">private</span> <span class="kd">final</span> <span class="nc">Date</span> <span class="n">endAt</span><span class="o">;</span>

	<span class="kd">public</span> <span class="nf">Period</span><span class="o">(</span><span class="nc">Date</span> <span class="n">startAt</span><span class="o">,</span> <span class="nc">Date</span> <span class="n">endAt</span><span class="o">)</span> <span class="o">{</span>
		<span class="k">this</span><span class="o">.</span><span class="na">startAt</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">Date</span><span class="o">(</span><span class="n">startAt</span><span class="o">.</span><span class="na">getTime</span><span class="o">());</span>
		<span class="k">this</span><span class="o">.</span><span class="na">endAt</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">Date</span><span class="o">(</span><span class="n">endAt</span><span class="o">.</span><span class="na">getTime</span><span class="o">());</span>
	<span class="o">}</span>

	<span class="kd">public</span> <span class="nc">Date</span> <span class="nf">getStartAt</span><span class="o">()</span> <span class="o">{</span>
		<span class="k">return</span> <span class="k">new</span> <span class="nf">Date</span><span class="o">(</span><span class="n">startAt</span><span class="o">.</span><span class="na">getTime</span><span class="o">());</span>
	<span class="o">}</span>

	<span class="kd">public</span> <span class="nc">Date</span> <span class="nf">getEndAt</span><span class="o">()</span> <span class="o">{</span>
		<span class="k">return</span> <span class="k">new</span> <span class="nf">Date</span><span class="o">(</span><span class="n">endAt</span><span class="o">.</span><span class="na">getTime</span><span class="o">());</span>
	<span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<p>생성자와 getter 에서 신규 Date 객체로 재생성해서 처리하고 있기 때문에 Period 클래스 내부의 필드를 변경할 수 없게 되었다. Date 가 불변클래스가 아니기 때문에 위와 같은 <strong>방어적 복사본</strong>이 적용된 로직이 필요하다.</p>

<h3 id="advantages-of-immutable-instance">Advantages of Immutable Instance</h3>

<p><strong>단순함을 통한 안전성</strong></p>

<p>불변객체는 생성된 시점에서 파괴될 때까지 상태를 그대로 간직한다. 그렇기 때문에 멀티 스레드 환경에서 동시에 사용해도 상태가 변하지 않는다. 동기화 작업없이 <strong>멀티 스레드 환경에서 안전하게 공유</strong>할 수 있다.</p>

<p><strong>Cache, Map, Set 등의 자료구조에 최적화</strong></p>

<p>Map 에서 Key 를 인식하기 위해 equals() 와 hash() 를 이용한다. 그런데 객체의 상태가 변하면 equals() 와 hash() 의 결과가 달라지기 때문에 문제가 발생한다.</p>

<ul>
  <li>Key 를 이용한 탐색 불가</li>
  <li>중복 저장</li>
</ul>

<p>그렇기 때문에 불변 객체가 Key 에 적합하다.</p>

<p><strong>실패 원자적인 메소드 (Failure Atomicity) 생성 가능</strong></p>

<p>메소드 실행 도중에 예외가 발생했을 경우, 객체 상태가 변경된 채로 남아 일광성을 잃는 경우가 있다.</p>

<blockquote>
  <p>실패 원자성 <strong>(Failure Atomicity)</strong>
작업이 실패했을 때, 시스템이 일관된 상태로 돌아가야함을 보장하는 성격</p>

</blockquote>

<p>실패 원자적이지 못한 코드의 예제는 아래와 같다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">public</span> <span class="kd">class</span> <span class="nc">Account</span> <span class="o">{</span>

	<span class="kd">private</span> <span class="kt">int</span> <span class="n">balance</span><span class="o">;</span>

	<span class="kd">public</span> <span class="nf">Account</span><span class="o">(</span><span class="kd">final</span> <span class="kt">int</span> <span class="n">balance</span><span class="o">)</span> <span class="o">{</span>
		<span class="k">this</span><span class="o">.</span><span class="na">balance</span> <span class="o">=</span> <span class="n">balance</span><span class="o">;</span>
	<span class="o">}</span>

	<span class="kd">public</span> <span class="kt">void</span> <span class="nf">withdraw</span><span class="o">(</span><span class="kd">final</span> <span class="kt">int</span> <span class="n">amount</span><span class="o">)</span> <span class="o">{</span>
		<span class="k">if</span> <span class="o">(</span><span class="n">amount</span> <span class="o">&lt;=</span> <span class="mi">0</span><span class="o">)</span> <span class="o">{</span>
			<span class="k">throw</span> <span class="k">new</span> <span class="nf">IllegalArgumentException</span><span class="o">(</span><span class="s">"Amount must be greater than zero"</span><span class="o">);</span>
		<span class="o">}</span>
		
		<span class="k">if</span> <span class="o">(</span><span class="n">amount</span> <span class="o">&gt;</span> <span class="n">balance</span><span class="o">)</span> <span class="o">{</span>
			<span class="k">throw</span> <span class="k">new</span> <span class="nf">RuntimeException</span><span class="o">(</span><span class="s">"Insufficient balance"</span><span class="o">);</span>
		<span class="o">}</span>
		<span class="k">this</span><span class="o">.</span><span class="na">balance</span> <span class="o">-=</span> <span class="n">amount</span><span class="o">;</span>
	<span class="o">}</span>

<span class="o">}</span>
</code></pre></div></div>
<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@Service</span>
<span class="kd">public</span> <span class="kd">class</span> <span class="nc">WithdrawService</span> <span class="o">{</span>

	<span class="kd">private</span> <span class="kd">final</span> <span class="nc">UserAccountRepository</span> <span class="n">userAccountRepository</span><span class="o">;</span>
	<span class="kd">private</span> <span class="kd">final</span> <span class="nc">UserAccountHistoryRepository</span> <span class="n">userAccountHistoryRepository</span><span class="o">;</span>

	<span class="nd">@Transactional</span>
	<span class="kd">public</span> <span class="kt">void</span> <span class="nf">withdraw</span><span class="o">(</span><span class="kd">final</span> <span class="nc">Account</span> <span class="n">account</span><span class="o">,</span> <span class="kd">final</span> <span class="kt">int</span> <span class="n">amount</span><span class="o">)</span> <span class="o">{</span>
		<span class="n">account</span><span class="o">.</span><span class="na">withdraw</span><span class="o">(</span><span class="n">amount</span><span class="o">);</span>
		<span class="n">userAccountRepository</span><span class="o">.</span><span class="na">withdraw</span><span class="o">(</span><span class="n">amount</span><span class="o">);</span>
		<span class="n">userAccountHistoryRepository</span><span class="o">.</span><span class="na">addHistory</span><span class="o">(</span><span class="n">account</span><span class="o">,</span> <span class="n">amount</span><span class="o">);</span>
	<span class="o">}</span>

<span class="o">}</span>
</code></pre></div></div>

<p>UserAccountHistoryRepository 작업하는 과정에서 예외가 발생했을 때, UserAccountRepository 는 트랜잭션을 통해 원복될 수 있지만, Account 객체의 상태는 원복되지 않는다. 이 코드는 실패 원자적이지 못한다.</p>

<p>실패원자적인 코드로 변경한다면 아래와 같다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">public</span> <span class="kd">class</span> <span class="nc">Account</span> <span class="o">{</span>

    <span class="kd">private</span> <span class="kd">final</span> <span class="kt">int</span> <span class="n">balance</span><span class="o">;</span>

    <span class="kd">public</span> <span class="nf">Account</span><span class="o">(</span><span class="kd">final</span> <span class="kt">int</span> <span class="n">balance</span><span class="o">)</span> <span class="o">{</span>
        <span class="k">this</span><span class="o">.</span><span class="na">balance</span> <span class="o">=</span> <span class="n">balance</span><span class="o">;</span>
    <span class="o">}</span>

    <span class="kd">public</span> <span class="kt">int</span> <span class="nf">getBalance</span><span class="o">()</span> <span class="o">{</span>
        <span class="k">return</span> <span class="n">balance</span><span class="o">;</span>
    <span class="o">}</span>

    <span class="kd">public</span> <span class="nc">Account</span> <span class="nf">withdraw</span><span class="o">(</span><span class="kd">final</span> <span class="kt">int</span> <span class="n">amount</span><span class="o">)</span> <span class="o">{</span>
        <span class="k">if</span> <span class="o">(</span><span class="n">amount</span> <span class="o">&lt;=</span> <span class="mi">0</span><span class="o">)</span> <span class="o">{</span>
            <span class="k">throw</span> <span class="k">new</span> <span class="nf">IllegalArgumentException</span><span class="o">(</span><span class="s">"Amount must be greater than zero"</span><span class="o">);</span>
        <span class="o">}</span>

        <span class="k">if</span> <span class="o">(</span><span class="n">amount</span> <span class="o">&gt;</span> <span class="n">balance</span><span class="o">)</span> <span class="o">{</span>
            <span class="k">throw</span> <span class="k">new</span> <span class="nf">RuntimeException</span><span class="o">(</span><span class="s">"Insufficient balance"</span><span class="o">);</span>
        <span class="o">}</span>

        <span class="k">return</span> <span class="k">new</span> <span class="nf">Account</span><span class="o">(</span><span class="k">this</span><span class="o">.</span><span class="na">balance</span> <span class="o">-</span> <span class="n">amount</span><span class="o">);</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<h3 id="disadvantages-of-immutable-instance">Disadvantages of Immutable Instance</h3>

<p><strong>성능 오버헤드</strong></p>

<p>불변객체는 상태가 변경될때마다 새로운 객체를 생성해야하므로 전체 메모리 사용량의 증가와 오버헤드를 발생시킬 수 있다. 또한, 가비지 컬렉션의 부담이 커질 수 있다는 문제가 발생할 수 있다.</p>

<p>그렇기 때문에 자주 사용되는 불변객체에 대한 풀을 생성하는 등의 방식을 고안해서 객체 재사용을 권장해야 한다.</p>

<p><img src="https://raw.githubusercontent.com/lkhlkh23/lkhlkh23.github.io/master/images/2024-07-22/1.png" alt="1.png" /></p>

<p>실제 개발을 하다보면 항상 불변 클래스로 개발하기가 쉽지 않다. <strong>불변할 수 없다면, 변경 가능성을 최소화해서 최대한 불변 클래스에 가깝게 만들자!</strong></p>]]></content><author><name>lkhlkh23</name></author><category term="java" /><category term="java" /><category term="immutable" /><summary type="html"><![CDATA[final 을 써야하는 이유에 대해 포스팅을 하다가, 불변객체를 써야하는 이유까지 다시 복습하게 되었다. 다시 이펙티브 자바를 읽었다. 이 책은 정말 매력적이다. 읽을때마다 항상 새롭다. 늘 뇌가 리셋되었다고 할까?! 나의 뇌를 순수하게 만든다고 할까?!]]></summary></entry><entry><title type="html">자바에서 final 사용해야 하는 이유</title><link href="https://lkhlkh23.github.io/jekyll-theme-yat/java/2024/07/21/why-use-final-keyword.html" rel="alternate" type="text/html" title="자바에서 final 사용해야 하는 이유" /><published>2024-07-21T00:00:00+00:00</published><updated>2024-07-21T00:00:00+00:00</updated><id>https://lkhlkh23.github.io/jekyll-theme-yat/java/2024/07/21/why-use-final-keyword</id><content type="html" xml:base="https://lkhlkh23.github.io/jekyll-theme-yat/java/2024/07/21/why-use-final-keyword.html"><![CDATA[<p>팀원의 코드 리뷰를 하는 과정에서 <code class="language-plaintext highlighter-rouge">final 키워드를 습관적으로 작성하면 좋을 것 같다!</code> 라고 피드백을 주었다. 그리고 그 이유에 대해서는 명확하게 설명하지 못했다. 왜냐하면 내가 가장 좋아했던 개발자분의 코드에는 항상 <code class="language-plaintext highlighter-rouge">final</code> 있었기 때문에 나는 그 방식이 좋은 방식이라고 맹신했기 때문이다.</p>

<p><img src="https://raw.githubusercontent.com/lkhlkh23/lkhlkh23.github.io/master/images/2024-07-21/0.png" alt="0.png" /></p>

<p>그래서 나름대로 그 이유를 생각해봤다!</p>

<h3 id="why-should-we-use-final-">Why should we use final .?!</h3>

<p><code class="language-plaintext highlighter-rouge">final</code> 키워드를 통해 <code class="language-plaintext highlighter-rouge">안전성</code>, <code class="language-plaintext highlighter-rouge">명확한 설계의도</code> 그리고 <code class="language-plaintext highlighter-rouge">성능 최적화</code> 라는 장점을 얻을 수 있다.</p>

<p>아래 코드를 보자!</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">public</span> <span class="kd">class</span> <span class="nc">DriveAnalyzer</span> <span class="o">{</span>

	<span class="kd">public</span> <span class="kt">void</span> <span class="nf">analyze</span><span class="o">(</span><span class="kt">int</span> <span class="n">distacne</span><span class="o">,</span> <span class="kt">int</span> <span class="n">errorDistance</span><span class="o">)</span> <span class="o">{</span>
		<span class="c1">// 운전 시간 계산은 거리를 통해 계산</span>
		<span class="kt">double</span> <span class="n">averageSpeed</span> <span class="o">=</span> <span class="mf">60.0</span><span class="o">;</span> 
		<span class="kt">double</span> <span class="n">drivingTime</span> <span class="o">=</span> <span class="n">distacne</span> <span class="o">/</span> <span class="n">averageSpeed</span><span class="o">;</span>

		<span class="c1">// 연료비 계산은 (거리 - 오차 거리) 를 통해 계산</span>
		<span class="kt">double</span> <span class="n">fuelEfficiency</span> <span class="o">=</span> <span class="mf">15.0</span><span class="o">;</span> 
		<span class="kt">double</span> <span class="n">fuelPrice</span> <span class="o">=</span> <span class="mf">1.5</span><span class="o">;</span> 
		<span class="n">distacne</span> <span class="o">=</span> <span class="n">distacne</span> <span class="o">-</span> <span class="n">errorDistance</span><span class="o">;</span>
		<span class="kt">double</span> <span class="n">fuelUsed</span> <span class="o">=</span> <span class="o">(</span><span class="n">distacne</span><span class="o">)</span> <span class="o">/</span> <span class="n">fuelEfficiency</span><span class="o">;</span>
		<span class="kt">double</span> <span class="n">fuelCost</span> <span class="o">=</span> <span class="n">fuelUsed</span> <span class="o">*</span> <span class="n">fuelPrice</span><span class="o">;</span>

		<span class="c1">// 운전 점수는 (거리 - 오차 거리) 통해 계산</span>
		<span class="kt">double</span> <span class="n">maxDistance</span> <span class="o">=</span> <span class="mf">100.0</span><span class="o">;</span>
		<span class="kt">double</span> <span class="n">maxScore</span> <span class="o">=</span> <span class="mf">90.0</span><span class="o">;</span>
		<span class="kt">double</span> <span class="n">drivingScore</span> <span class="o">=</span> <span class="o">(</span><span class="n">distacne</span> <span class="o">&lt;=</span> <span class="n">maxDistance</span><span class="o">)</span> <span class="o">?</span> <span class="n">maxScore</span> <span class="o">:</span> <span class="n">maxScore</span> <span class="o">*</span> <span class="o">(</span><span class="n">maxDistance</span> <span class="o">/</span> <span class="n">distacne</span><span class="o">);</span>

		<span class="c1">// 결과 출력</span>
		<span class="nc">System</span><span class="o">.</span><span class="na">out</span><span class="o">.</span><span class="na">println</span><span class="o">(</span><span class="s">"Fuel Cost: "</span> <span class="o">+</span> <span class="n">fuelCost</span><span class="o">);</span>
		<span class="nc">System</span><span class="o">.</span><span class="na">out</span><span class="o">.</span><span class="na">println</span><span class="o">(</span><span class="s">"Driving Score: "</span> <span class="o">+</span> <span class="n">drivingScore</span><span class="o">);</span>
		<span class="nc">System</span><span class="o">.</span><span class="na">out</span><span class="o">.</span><span class="na">println</span><span class="o">(</span><span class="s">"Driving Time: "</span> <span class="o">+</span> <span class="n">drivingTime</span><span class="o">);</span>
	<span class="o">}</span>
<span class="o">}</span>

</code></pre></div></div>

<p>위의 코드를 간단하게 설명하자면, 거리와 오차거리라는 파라미터를 받아서, 운전시간과 연료비 계산 그리고 운전 점수를 계산하는 3개의 역할을 수행하는 코드이다. <code class="language-plaintext highlighter-rouge">final</code> 키워드와 관련된 코드의 문제점을 나열해보겠다.</p>

<p><strong>왜곡된 설계의도</strong></p>

<p><code class="language-plaintext highlighter-rouge">averageSpeed</code> 는 모든 차량의 평균속도로서 상수와 같은 역할을 수행한다. 하지만, <code class="language-plaintext highlighter-rouge">final</code> 이라는 키워드가 없기 때문에 코드를 유지보수하는 사람은 언제든지 변할 수 있는 변수로 이해할 수 있다. 처음 설계의 의도가 다음 개발자에게 전달되지 않는 문제가 발생한다.</p>

<p><strong>코드의 불안전성</strong></p>

<p>위의 코드는 잠재적인 버그를 가진 코드이다. 메소드에서 3개의 역할을 수행하고 있는데, distacne 입장에서 각각의 역할이 순서에 의존적이다. 운전 시간은 <code class="language-plaintext highlighter-rouge">disatnce</code> 를 통해 계산하지만, 연료비 계산과 운전점수는 <code class="language-plaintext highlighter-rouge">distacne - errorDistance</code> 를 이용해서 계산한다. 만약 운전시간 계산이 추가 개발을 통해 요청된 사항이고, 메소드 가장 끝에 있었다면 분명 문제가 발생할 것이다. 그리고 추가 개발을 담당한 개발자는 <code class="language-plaintext highlighter-rouge">disatnce</code> 가 불변이 아닐 수 있기 때문에 코드를 모두 분석해서 <code class="language-plaintext highlighter-rouge">disatnce</code> 에 대해서 완전하게 이해한 후 작성을 해야할 것이다. 결국은 기능의 순서에 의존적이기에 코드의 안전성이 떨어진다.</p>

<p><strong>리팩토링의 불편함</strong></p>

<p><code class="language-plaintext highlighter-rouge">analyze</code> 메소드는 3개의 역할을 수행하고 있기 때문에 많은 역할을 수행하고 있다. 그렇기 때문에 테스트하기도 어려운 구조이다. 이런 코드를 보게된다면 대부분의 개발자라면 <code class="language-plaintext highlighter-rouge">option + cmd + m</code> 단축키를 수행할 것이다.  <code class="language-plaintext highlighter-rouge">extract method</code> 를 수행해서 주석을 기준으로 세개의 메소드를 생성할 것이다. 하지만, 문제가 발생한다. <code class="language-plaintext highlighter-rouge">distance</code> 는 불변하지 않기 때문에 운전점수 계산을 위해 생성한 메소드에서는 <code class="language-plaintext highlighter-rouge">distacne - errorDistance</code> 를 확인하고 반드시 수동으로 넣어야 한다. 항상 앞에 있는 로직을 완전하게 확인하고 작업을 해야한다.</p>

<p>물론 위의 코드는 문제점을 들어내기 위해 작성된 코드이기 때문에 억지스러울 수 있지만, 실제로 위와 비슷한 구조의 더 장문의 코드를 본적이 있다. 그리고 리팩토링을 할 때 많은 어려움을 가졌다.</p>

<p>그렇다면 이제 <code class="language-plaintext highlighter-rouge">성능 최적화</code> 라는 장점을 간단하게 설명하려고 한다. <code class="language-plaintext highlighter-rouge">final</code> 키워드는 컴파일러가 최적화할 수 있는 힌트를 제공한다. 예를 들어, <code class="language-plaintext highlighter-rouge">final</code> 메서드는 오버라이딩될 수 없으므로, 컴파일러는 이를 인라인으로 최적화할 수 있다. 여기서 <code class="language-plaintext highlighter-rouge">인라인으로 최적화</code> 라는 단어가 조금 이해가 되지 않을 수 있다. 아래와 같은 코드가 있다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">public</span> <span class="kd">final</span> <span class="kd">class</span> <span class="nc">Math</span> <span class="o">{</span>
    <span class="kd">public</span> <span class="kd">final</span> <span class="kt">int</span> <span class="nf">square</span><span class="o">(</span><span class="kt">int</span> <span class="n">x</span><span class="o">)</span> <span class="o">{</span>
        <span class="k">return</span> <span class="n">x</span> <span class="o">*</span> <span class="n">x</span><span class="o">;</span>
    <span class="o">}</span>
<span class="o">}</span>

<span class="kd">public</span> <span class="kd">class</span> <span class="nc">Test</span> <span class="o">{</span>
    <span class="kd">public</span> <span class="kd">static</span> <span class="kt">void</span> <span class="nf">main</span><span class="o">(</span><span class="nc">String</span><span class="o">[]</span> <span class="n">args</span><span class="o">)</span> <span class="o">{</span>
        <span class="nc">Math</span> <span class="n">math</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">Math</span><span class="o">();</span>
        <span class="kt">int</span> <span class="n">result</span> <span class="o">=</span> <span class="n">math</span><span class="o">.</span><span class="na">square</span><span class="o">(</span><span class="mi">5</span><span class="o">);</span> 
        <span class="nc">System</span><span class="o">.</span><span class="na">out</span><span class="o">.</span><span class="na">println</span><span class="o">(</span><span class="n">result</span><span class="o">);</span>
    <span class="o">}</span>
<span class="o">}</span>

</code></pre></div></div>

<p>인라인으로 최적화를 수행하면 다음과 같이 된다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">public</span> <span class="kd">class</span> <span class="nc">Test</span> <span class="o">{</span>
    <span class="kd">public</span> <span class="kd">static</span> <span class="kt">void</span> <span class="nf">main</span><span class="o">(</span><span class="nc">String</span><span class="o">[]</span> <span class="n">args</span><span class="o">)</span> <span class="o">{</span>
        <span class="kt">int</span> <span class="n">result</span> <span class="o">=</span> <span class="mi">5</span> <span class="o">*</span> <span class="mi">5</span><span class="o">;</span> <span class="c1">// 메서드 호출 대신 실제 코드 삽입</span>
        <span class="nc">System</span><span class="o">.</span><span class="na">out</span><span class="o">.</span><span class="na">println</span><span class="o">(</span><span class="n">result</span><span class="o">);</span>
    <span class="o">}</span>
<span class="o">}</span>

</code></pre></div></div>

<p>메소드 호출의 오버헤드를 감소시켜서 성능 최적화에 도움이 된다. 하지만 이 부분은 크게 공감되지는 않는다. <code class="language-plaintext highlighter-rouge">final</code> 키워드가 있다고해서 모두 인라인 최적화가 진행되지 않고, 조건이 존재하기 때문이다.</p>

<p><code class="language-plaintext highlighter-rouge">final</code> 을 써야하는 다른 개발자들의 의견을 봤을 때, 아래와 같은 의문을 제기하시는분도 계셨다.</p>

<p>Question : IDE 에서 자동화로 만드는 코드에 기본적으로 final이 선언되지 않을까?</p>

<p>순간 이 의문에 대해서 답변이 생각나지 않았다. 정말 똑똑한 사람들이 고민하고 개발한 결정체에서 왜 자동으로 제공하지 않지?! 라는 의문에 나도 빠졌다. 그리고 고민한 결과 내가 내린 답변은 아래와 같다.</p>

<p>자동완성에서 <code class="language-plaintext highlighter-rouge">final</code> 이 필수가 아닌 이유는, 개발자가 상황에 따라 유연하게 코드를 작성할 수 있도록 개발자에게 선택권을 부여한 것이라고 생각한다. <code class="language-plaintext highlighter-rouge">final</code> 이 필수적이라고 한다면, 이를 제거하는 과정이 필요할 수 있으며 개발자에게는 불편함과 번거로움을 줄 수 있다. IDE 는 개발 생산성 향상에 도움을 주기 위한 목적으로 개발된 도구이기 때문에 이러한 이유에 적합하지 않다. 그렇기 때문에 개발자의 선택권을 위해 제공하지 않는다고 생각한다.</p>

<p>다음에는 불변객체를 사용해야 하는 이유에 대해 정리를 해봐야 겠다! 이전에 이펙티브 자바에서 읽었던 기억이 있는데, 내용은 기억나지 않는다.</p>]]></content><author><name>lkhlkh23</name></author><category term="java" /><category term="java" /><category term="final" /><summary type="html"><![CDATA[팀원의 코드 리뷰를 하는 과정에서 final 키워드를 습관적으로 작성하면 좋을 것 같다! 라고 피드백을 주었다. 그리고 그 이유에 대해서는 명확하게 설명하지 못했다. 왜냐하면 내가 가장 좋아했던 개발자분의 코드에는 항상 final 있었기 때문에 나는 그 방식이 좋은 방식이라고 맹신했기 때문이다.]]></summary></entry></feed>